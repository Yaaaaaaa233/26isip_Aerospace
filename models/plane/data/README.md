# Plane 数据目录

本目录按数据生命周期分层：

- `raw/`：用户/台架/飞行原始文件，只读保存，不覆盖、不改写数值；
- `processed/`：清洗、时间对齐、单位确认后的数据；
- `calibration/`：由原始数据辨识得到、带适用范围和留出验证记录的参数。

## 勘误：`raw/功率-升力.xlsx` 已按方案 2 前向移除（2026-09-08）

**勘误内容**：该文件（2026-09-02 提供的 5 点 Excel，字段未确认）属于内部参考数据，此前被跟踪入库，与"内部参考数据禁止上 git"的治理规则冲突。经叶安 2026-09-08 拍板选择**方案 2（前向移除 + 勘误）**：自本提交起文件不再出现在仓库中，原件与台架表/电芯报告/协议一并存放于仓库外 `第二阶段\参考数据\功率-升力.xlsx`。

- 原件 SHA-256：`01fd95609f6aa7ca004875c060efa0ddcbebcb25a81ca92b726a07547428b1b3`（移出前后字节未动，本次仅移动位置）；
- git 历史：由 `66a1d5b` 引入，**历史提交中的旧副本保留不销毁**——仓库禁用 force-push，不做历史改写；本勘误即为完整来龙去脉登记；
- 使用限制继续有效：在字段、单位、采样条件、传感器和时间基准由数据提供者确认前，不得把它（或其副本）接入 `plane.config`，不得标记为 `calibrated`，不得据此生成真实节能结论；若日后确认可用，走正常清洗流程（`processed/` + 哈希 + 清洗记录），原件仍留仓库外；
- 处置依据：`docs/architecture/06_platform_power_environment_model.md` §4 治理检查点、§8 登记事项 5；决策与执行记录见 `docs/worklog/2026-09-08-zcode-xlsx-erratum.md`。

## 目录现状

- `raw/`：空（原件一律存仓库外 `第二阶段\参考数据\`，仓库内只进清洗记录与哈希）；
- `processed/`：`bench_motor_mn1005_cleaning.md`（电机台架 443 行表清洗记录）；
- `calibration/`：`bench_motor_mn1005_fit.{json,md}`（电机分块拟合）、`battery_model_v1.{json,npz}` + `battery_cell_v1.md`（电池形状先验模型）。
