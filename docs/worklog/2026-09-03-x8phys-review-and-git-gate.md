# 2026-09-03 X8PHYS 审核与 Git 门禁

项目组：周航正、霍奕茗、于跃、叶安、王健祺
文件负责人：待组内确认
本次贡献：周航正（审核与数据隔离要求）；Codex（审查、修复、验收和证据整理）
审核：待项目组审核、待指导教师确认
AI协助：Codex

## 本次做了什么

- 按 `X8PHYS_README.md` P0--P4 审核对象、适配器、Plane 代理、测试、数据追溯和结论边界。
- 修复施加 PWM、功率限制/平衡、截止 OCV、有限性校验、物理风边界、圆周运动学和失效测试问题。
- 主目录三份源数据按精确路径加入 `.gitignore`；未暂存、未提交、未上传。
- 新增 `docs/evidence/X8PHYS_REVIEW_20260903.md`，完整记录门槛、实跑结果和未闭合项。

## 关键决策与理由

- 修订 2026-09-03 `x8phys-data-path` 简报中的回归数字：旧数字来自不同动态状态，不作为同状态风对比；新基线固定为 800 rpm 同状态 597.490/961.047 W。
- 物理 Plane 读取 `wind_truth_ne_mps`；测量风只允许进入控制器上下文，避免传感器失效改变真实物理风。rebase 到接口字典 0.3 后，进一步把 PathCommand 从 WindSample 中拆出。
- 7S1P 继续标为 proxy。P0 拟合通过不等于整机电池配置或动态桨效应已校准。

## 遗留问题 / 风险

- `run_air_m0b_safety_injection` 三次在首场景停滞；`run_air_m1_robustness` 在首个噪声注入场景停滞，均未得到完整当次 PASS。
- M0-C trials 完成全部 PASS 输出后，MATLAB 退出清理停滞；本机 R2022b 会话稳定性问题仍在。
- 统一 Harness、S/P 确认、动态 `Ct/Cp(J)`、双桨干扰、整机质量/阻力和温度模型仍待完成。

## 下一步

- 用独立最小复现定位 `SimulationCommand=update` 与内存注入场景的卡死，不在未覆盖错误路径矩阵时直接修改验收基础设施。
- 在独立问题中最小复现 M0-B/M1 注入卡死；按 ADR-003 将其登记为环境层 OPEN LIMITATION，不倒推本次对象功能层失败。

## 验收状态

- X8PHYS/Plane/M0-C 单元/M2 单元/M0-A 旁路/M0-C trials/M2 c3：通过，详细值见证据报告。
- M0-B 安全注入/M1 完整矩阵：未完成（R2022b 注入场景停滞）。
- 仓库治理与 diff 检查：通过。
- 上传前已 rebase 到 `origin/main` 的 `e5d57456559d148783544eb9c936c0e92c23d2a6`；远端新增 M3 仲裁文件与本工作线无文件重叠。
