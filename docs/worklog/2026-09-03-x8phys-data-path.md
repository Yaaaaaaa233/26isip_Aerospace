# 2026-09-03 X8PHYS 数据接入与技术路径

项目组：周航正、霍奕茗、于跃、叶安、王健祺
文件负责人：待组内确认
本次贡献：周航正（提出“指令+风→运动/耗电”目标并提供数据）；Codex（数据读取、对象实现、验收标准和追溯文档）
审核：待项目组审核、待指导教师确认
AI协助：Codex

## 本次做了什么

- 读取 PAW 40x13.1R 静推表和 ZE12070117A-024Ah 电芯报告，保留原始文件，不改写数值。
- 将静推拟合参数接入 `+x8phys`：推力、轴功率、电功率和 KV；加入电机一阶执行动态。
- 将电池对象改为 7S1P 待确认 proxy：OCV 查表、内阻压降、库仑积分、截止保护。
- 在 `X8PHYS_README.md` 写明 P0--P4 技术路径、验收门槛和不越界结论；新增数据追溯文档。

## 关键决策与理由

- 不修改冻结 `.slx`、ESC/RL 因果签名或 `platform_step` 的 M0 字段。
- 静推数据只支撑空速 0 的拟合；非零风和动态桨效应继续标记为 proxy 外推。
- 电芯报告没有确认整机 S/P 配置，因此 7S1P 只作为可覆盖的待确认默认值，不作为真实电池结论。

## 遗留问题 / 风险

- 已在 MATLAB R2022b 执行对象验收；对象与平台适配均通过。
- 仍缺动态 `Ct/Cp(J)`、双桨干扰、整机质量/阻力面积、实际 S/P 和温度模型。

## 下一步

- 继续在 MATLAB R2022b 记录对象/平台回归结果与参数版本。
- 确认电池串并联和整机参数后，建立 processed/calibration 数据包和留出验证。
- 独立 Harness 接入 `air_spare` 最终 PWM 与风场，保持冻结快照只读。

## 验收状态

- `run_x8phys_acceptance`：通过；对象/平台均 PASS（对象零风 1341.268 W，20 m/s 风 895.122 W，数值仅作代理回归基线）。
- `run_plane_acceptance`：通过。
- `harness/run_acceptance`：未实现，按其现有代码硬失败；不是本次回归失败。
- 静态代码/数据追溯检查：已完成。
- `tools/check_repo_governance.ps1`：通过（使用 `pwsh` 运行）。
