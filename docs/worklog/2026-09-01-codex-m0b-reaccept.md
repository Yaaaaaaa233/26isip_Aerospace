# 2026-09-01 Codex M0-B 修复版独立复验

## 本次做了什么

- fetch 确认 `main/origin/main=00fd67e`，外层本地与仓库的四个模型、五份核心脚本哈希一致。
- 独立 MATLAB R2022b 复跑基线、名义/扰动速度测试、四类安全注入，全部通过。
- 额外检查两个模型各 11 条精确源端口连线、日志形状/有限性/采样时间/指令边界及限速、PWM 注入时 RPM 位送达。
- 对保存快照 `air_m0b` 补做姿态位 3 保护、13 s 功率故障清除后的完整恢复；均通过。
- 新增证据报告 `docs/evidence/M0B_REACCEPT_CODEX_20260901.md` 并回写开发状态。没有修改模型或正式实现/测试脚本，没有 commit/push。

## 关键决策与理由

- **当前修复版 M0-B 阶段验收通过**；旧 `452237f` 的不通过报告保留为历史。原始 flags 断线修复有效，当前有独立动态证据支撑，不再仅依靠原脚本 PASS。
- 真实 Ve/PWM/quat 旁路最大差为 0；名义 9 m/s 平均误差 0.031533 m/s。快照功率故障恢复在 9.001 s 释放、11 s 重新 active，12–13 s 平均误差约 0.099515 m/s。

## 遗留问题 / 风险

- 扰动均值误差仍约 1.6 m/s，不能将名义场景通过扩展为扰动下精确跟踪；物理模型/功率校准仍未完成。
- M0-C 文档状态 1/2 语义有歧义，成本采样应排除 warmup；进入实现前需统一。
- 建议正式注入测试延至重新 active，收紧安装器 `verifyLink` 的具体源端口断言，清理旧悬空支线警告；本次已用独立检查补证当前快照，但未实施这些改进。

## 下一步

- 按路线推进 M0-C 名义场景的速度 ESC 接口与稳定窗口设计，保留保护层、固定参考配对基线与故障回归；不接 eta 分配器或 RL。

## 验收状态

- `run_air_m0a_baseline_compare`：PASS。
- `run_air_m0b_tests`：4/4 PASS。
- `run_air_m0b_safety_injection`：4/4 PASS。
- 独立快照姿态保护、功率完整恢复与日志/端口检查：PASS。
- `modules/ratio_esc/run_acceptance`：未运行，该线无变更且本轮不提交。
