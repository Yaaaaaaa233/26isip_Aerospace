# 2026-09-01 M0-B 复核缺陷修复与重新验收（ZCode 会话简报）

## 背景

独立复核（[`../evidence/M0B_REVIEW_20260901.md`](../evidence/M0B_REVIEW_20260901.md)）否定 2026-08-31 的 M0-B 验收：P1 安全标志链路断线（flags_raw 悬空，位 1/2/4/6/7 恒 0，故障注入证明保护不动作）+ 四项验收口径缺口。修复计划先按规范落入路线图 §6 并提交（`97c641e`），复核证据与文档修订入库（`c123703`），随后执行修复。

## 修复与结果（证据：[`../evidence/M0B_RERUN_20260901.md`](../evidence/M0B_RERUN_20260901.md)）

1. **P1**：安装器重定向改为按目标逐支路删除（保留 flags_raw）；新增**保存后重载验证**（磁盘重载 + 67 条关键连线断言）。从 `air_m0a.slx` 恢复重装全层。
2. **Ve 勘误**：compare 脚本改标 6DOF 输出 1 + `[N×3]` 维度断言；旁路三信号（含真 Ve）与 `air` 差 0（`20260901_003133`）。
3. **验收双口径**（`20260901_003714`）：N1 名义（roll 正弦置 0）9 m/s 均值误差 **0.032 m/s**、失跟 0%、干净窗 4.0 s；D1/D2 扰动 1.615/1.598 m/s（与复核复跑逐位一致）；S2 阶跃 pre 窗修复（2.134 有限值）。
4. **逐位故障注入**（`20260901_005201`，新脚本 `run_air_m0b_safety_injection.m`）：位 1/4/6/7 全部"active 先行→原始位触发→下游位触发→frozen 0.000 s→fallback 0.500 s"；位 6 脉冲清除后 9.0 s 释放回退并重新爬坡（恢复链验证）。
5. 快照 `air_m0b.slx` 已替换为修复版；M0-C 交付清单已恢复至路线图 §6。

## 新增工程经验（并入 M0B_SPEED_LOOP.md §4）

- **删线守则**：绝不全量删除源端口目标；按"源→具体目标端口"逐支路删。
- **保存后必须重载验证**：本环境静默断线只在编译/保存后显形。
- **Switch 控制口在测试内联使用同样不可靠**（NaN 注入第一版用 Switch 失败）；定时 NaN 用 `(s−1)/(s−1)` 除法技巧生成（故障前=1 无害、故障后 0/0=NaN）。
- "全零"型验收必须先用注入证明不是结构性为零。

## 会话交接

- 本地脚本：`models/px4_x8/{add_air_m0b_speed_loop,add_air_m0b_controller_gains,run_air_m0b_tests,run_air_m0b_safety_injection,run_air_m0a_baseline_compare}.m` 均已与仓库同步。
- 下一步 M0-C 按路线图 §6 执行；每次结构变更后重跑旁路 compare 与注入回归。
