# worklog 2026-09-08 — 全项目重新验收扫描（ZCode / 平台线，叶安指示）

## 做了什么

对 HEAD `949eb50`（P2 WP1–WP4 落地后）做全项目 fresh 验收扫描：治理、Plane 契约、P2 链检查（新增 G-U U 形门）、适配器、harness 单测、T1 入口@HEAD 演示、x8phys、M3 协调单测、M2/M3 驱动测试、**M3 正式批次 11 阶段在 HEAD 复跑**、算法线 17 个 MATLAB 入口 sweep。结果与发现全文见 [PROJECT_REACCEPT_SWEEP_20260908](../evidence/PROJECT_REACCEPT_SWEEP_20260908.md)。

要点：

- **M3 正式批次在 HEAD 复跑 PASS**（batchId `000f024f…`，11 阶段全 attempt 1，14 臂复核、恢复矩阵 28 行、配对数值与第三轮独立口径一致）——P2 上库未影响 M3 链；
- **1 项 P1 开放**：P2 合成功率无 U 形左支（H3 未接入，v\*=0）——已机器化为 G-U 门（FAIL 如实呈现），**T2 正式跑批冻结至修复**；修复方案（动量理论诱导修正缺省）待叶安拍板；
- T1 入口@HEAD 18/21，3 项 FAIL 均为模型更换预期签名（A3 内核动态、S1 proxy→P2 基线差 +77%）；
- 测试层时序竞态修复（M2/M3 驱动测试 mock 垫 15 ms，4 连跑稳定全过）；教训：ps1 注释一律 ASCII（无 BOM 文件被 PS5.1 按 ANSI 误读）；
- 登记开放项：M2 52 行链在 HEAD 未重跑（m2_eta_esc 冻结后被 M3 改动，复跑决定权归项目组/M2 独立验收制度）；算法线 ratio_esc/speed_esc 入口重存 .slx 的非幂等副作用（本次已恢复，请 owner 关注）。

## 如实记账

- 扫描期间两次流程性错误自纠：中文注释破坏 ps1 语法（改 ASCII）；sweep 副作用 .slx 变更（git checkout 恢复，未入库）；
- M3 批次期间工作树冻结执行到位（无仓库写入）；驱动测试时序问题的两次 FAIL 如实保留在扫描记录中，未隐藏。

## 下一步

1. 叶安拍板 H3 修复形式 → 实现 → G-U 转绿 → 清单 v1.1 正式登记门槛（解冻 T2 跑批前置）；
2. M3 独立复验（材料含本扫描 §1 #11 的 HEAD 复跑批次）；
3. §2.4/§2.5 开放项通报项目组。
