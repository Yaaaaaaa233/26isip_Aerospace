# 2026-09-02 任务6自适应算法库并入 + 路线口径环境风场调度模块

## 本次做了什么

- 同步远端（协作者删除了 Python 演示包 `11d3a8e`）；对照 `docs/TASKS_1_5_ROUTE.md` §3-5 与 `docs/ARCHITECTURE_MOP_MOE.md` 复核既有上传，确认**偏差**：此前并入的 wind_circle/sin_wind/ortho_wind 三模块对象侧是"风致功率曲线平移代理"（算法线研究），未覆盖路线要求的空速物理调度交付项。已在 README/DEVELOPMENT_STATUS 如实改标注（"对象侧为风致曲线平移代理；路线口径交付见 wind_field_sched"），不删除历史模块。
- 并入 `modules/adaptive_search`（任务6）：spsa占空比随机扰动 / bayes GP代理 / qnewton割线牛顿（两相制，新推荐）+ 双层MOP/MOE（MOP性能量×MOE任务效能分层，对齐 ARCHITECTURE_MOP_MOE 口径，旧字段全兼容）；28/28 测试、10/10 门槛；面板结果卡片移至日志栏上方。qnewton 风场尾段 0.272（tracker 0.805）、1h MOE 0.987（ea 0.975）。
- 新建 `控制寻优/wind_field_module`（本地工作区）并并入 `modules/wind_field_sched`：按路线§3-5交付口径实现空速物理对象（P=P0(|v·t̂+w|)，V*=6.3/P*=0.913 三次代理）、解析调度 v*(t)、DP验证（无限速=解析 3.9e-6；限速=加速度接口）、最优匀速对照、逐时刻可行性（|w⊥|≤V*）、滑窗LM在线风估计+观测性激励、任务4敏感性扫描+带宽准则、任务5三档信息结构对比；15/15 测试、8/8 门槛；三模块面板复用环境模型（航迹+风矢量）与飞机模型（双表盘）。

## 关键决策与理由

- **不改历史模块、只改标注**：wind 系列三模块的算法线结论（EA分级监测等）仍成立，其对象口径与路线交付口径不同——用改标注而非删除来消除歧义。
- **qnewton 两相制**（宽stencil全局下降+小stencil牛顿+割线曲率自校正）：涟漪谷深0.034、宽±0.7，宽探针必停在谷外二次平台（单步能耗≈4%），小stencil(±0.25)停在谷内（≈0.5%）——精度与能耗同时成立的结构性原因。
- **wind_field_sched 的 known 策略用真风**：它是信息结构上界参照（"若风已知能省多少"的标尺），面板与报告均标注"非因果算法"；online/blind 严格黑箱因果。
- **带宽准则按数据重述**：扫描显示"快风需高带宽"的先验假设不成立（长窗把快风平均化为均值风估计仍稳健）；准则改为数据支持的"短窗(<半圈)病态、长窗(W≥90)中位超额2.2% vs 短窗6.0%、中频区为谐振局限"。

## 遗留问题 / 风险

- 在线风估计在中频风区(ω≈0.2–0.35)存在谐振区，需要结构化估计（相位查表）——已在报告记录为下一步。
- bayes（GP代理）时变尾段跟踪劣于占空比法，如实横评不设门槛。
- tracker 在风场横评中的能耗优势来自平坦无噪设计点（对象口径不同），文档已标注。

## 下一步

- wind_field_sched：相位查表结构化风估计（谐振区改进）；限速DP接入加速度限制场景。
- 两条线均为代理对象口径，任何真实 X8 节能结论仍不可引用（红线3）。
