# 2026-09-02 M2 第四轮独立验收

## 本次做了什么

- 核对 `origin/main` 与本地版本，验收基线为主库 `2471917`，M2 修复来自 `239c19b`。
- 在 MATLAB R2022b GUI 实际运行 `verify_m2_round4_closure`；10 行仓库矩阵均 PASS，归档 `results/round4_closure/20260902_104606/`。
- 核对同会话两次九场景归档 `20260902_104000`、`20260902_104321`，两份 `pairs.csv` SHA-256 完全相同。
- 用真实受控失败直接验证 `test_m2_eta_esc_unit` 的 global 恢复与 persistent fresh，`maxDiffFresh=0`。
- 从非空调用者 global 运行总验证器，发现验证器正常返回时把调用者状态清空。
- 形成 `docs/evidence/M2_REACCEPT_ROUND4_CODEX_20260902.md` 并同步当前状态文档。

## 关键决策与理由

- 第三轮 R3-F1/R3-F2 的核心修复判为通过：三入口函数化及真实错误恢复均有动态证据。
- 第四轮总体判为部分通过：新增总验证器自身违反全局状态恢复规则，persistent 自动检查是间接推断，10 行矩阵也不是规则所称的完整笛卡尔覆盖。
- 保留 M2 模型/数值成果放行，不把验收工具缺陷外推为算法或 `.slx` 失效；同时撤回“自动化完全闭环/全规则满足”的强结论。

## 遗留问题 / 风险

- `verify_m2_round4_closure` 需快照并在正常/异常路径恢复调用者两个 M2 global。
- persistent fresh 直接比较应进入自动验证器和归档。
- 应扩展完整入口状态 × 退出路径矩阵，或收窄文档措辞。

## 下一步

- 由实现方关闭第四轮报告 R4-F1--F3，随后做针对性复验。
- M3 方案和实现可继续；下一个里程碑验收前先保证总验证器符合 v1.0 规则。

## 验收状态

- `verify_m2_round4_closure`：仓库内部 10/10 PASS，但调用者状态契约 FAIL。
- M2 核心三入口：通过。
- 第四轮总体：部分通过。
