# Worklog：M2 第三轮独立复验（Codex，2026-09-02）

项目组：周航正、霍奕茗、于跃、叶安、王健祺

文件负责人：叶安

记录人：待项目组确认；Codex（复验执行与文档代拟）

## 本次做了什么

- 核对主库最新提交 `bbc5990` 与第二轮报告关闭条件。
- 在 MATLAB R2022b GUI 中，用第二轮原样脏入口连续运行两次 `run_m2_session_chain`，两次均通过。
- 核对两次九场景 `summary.csv` / `pairs.csv` 哈希，确认逐位一致。
- 用非破坏性构造结果验证链尾 `air:M2Session:TrialsFailed` 硬断言。
- 用 MATLAB 断点在内存中制造单元测试 U1 断言失败和完整链 compare 断言失败，实测错误路径状态恢复。

## 关键结果

- 正常链 2/2 PASS：M2 归档 `20260902_000152`、`20260902_000537`；S1/S2/S3 = −0.25985% / −0.29211% / −0.22617%。
- 两次 `summary.csv` SHA256 均为 `B293F0A1E926DC7927DFD823117B0CF1AA25BF3BC76CEED503C5FE377E36990B`；`pairs.csv` 均为 `1B1845668E68877C35D6F337E84344E26284300BE7B1480EAC8D49F281D8CB3E`。
- 单元测试错误后入口 `M2_ETA_APPLIED=0.913456789` 未恢复，残留为 1.0。
- 完整链错误后入口 `esc/0.87、0.87654321` 未恢复，残留为 `fixed/1.0、1.0`。
- 根因是三个验收入口仍为脚本，错误退出时脚本工作区中的 `onCleanup` 没有在错误边界执行。

## 关键决策与理由

- M2 数值与正常成功路径继续通过；工程自动化改回“部分通过”。
- R2-F2 保持关闭；R2-F1 的错误路径和 R2-F3 重新打开。
- M3 方案设计可以继续，`.slx` 结构集成在异常清理修复前暂缓。

## 遗留问题 / 风险

- 需要把链、试验和单元测试入口函数化，或以显式 try/catch 清理后 rethrow。
- 下一轮必须真实覆盖 compare、injection、trials 三类错误出口，不能只用代码审查替代。
- `run_air_m2_trials` 独立错误路径尚未单独动态注入，列为第四轮必验项。

## 下一步

- ZCode 按第三轮报告 §8 修复脚本生命周期问题。
- 修复后按 §9 做第四轮针对性验收，并保留背靠背双链回归。

## 验收状态

- M2 数值协议：通过。
- 正常成功路径：通过。
- 异常恢复：未通过。
- 总体：部分通过。
