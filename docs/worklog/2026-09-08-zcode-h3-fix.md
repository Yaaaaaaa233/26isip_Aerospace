# worklog 2026-09-08 — H3 修复：动量理论诱导修正接入，U 形恢复，G-U 转绿（ZCode / 平台线）

## 做了什么

叶安拍板"2.1 按照你说的来修复"后实施（修复方案 = 2026-09-08 全项目重验收扫描 §2.1 的处置建议）：

1. **实现**（接口不变，只动功率内部）：`plane.config` 新增 `prop_diameter_m=1.016`（PAW 40 寸，盘面积 A=πD²/4）与 `h3_induced_gain=1.0`；`plane.step` 每桨从静态台架电功率中扣除诱导节省 `s=T·(vi0−vi(v_air))`——`vi0=√(T/(2ρA))`、`vi(v)=√((v/2)²+T/(2ρA))−v/2`（v=0 时 s≡0，悬停严格保持台架值；共轴下桨节省同乘 (1+δ) 与 H5 一致）；适配器 `truth()` 镜像同式。
2. **G-U 门升级**：稳态扫描加密到 1 m/s 分辨率（settle 连续 4 s + 0.5 s 静默窗均值），新增 H3 敏感性报告行（gain 0.7/1.3）。
3. **复验**：`run_p2_chain_check` **23/23 门 PASS**——G-U 转绿（**v\*=5 m/s、P(v\*)=360.0 W、相对悬停 487.3 W 降幅 26.1%**）；悬停功率严格不变；饱和负向（满电零/亏电 1117 步精确封顶/指令位不亮）、全部恒等式、eta 差动与凸性方向无一回归；`run_plane_acceptance` PASS（风耦合方向反转体现左支：空速 1.2 m/s 479.6 W vs 3.8 m/s 360.5 W）；适配器检查 PASS（稳态保真 0.26–0.49%）；harness 单测通过。附带物理一致性：巡航功率下降使同一演示缩放下截止时刻后移（cut@2251→3303 步），符合预期。
4. **文档**：06 v1.2 §3.1 H3 行重写（形式/参数/实测/敏感性义务，等级仍为缺省+敏感性——修正输入全部公开物理）；T2 清单升 **v1.1**（G-U 转正式门槛 + §5 追加拍板 7）；重验收证据 §2.1 关闭；状态页第九次更新。前后对比图（仓库外）：`第二阶段\P2构建验证图_20260908\P_v_curve_H3修复前后.png`。

## 人工核对表（叶安可视化验收用，参数应然值 vs 实测）

| 核对点 | 应然 | 实测 | 判定 |
|---|---|---|---|
| 悬停不动性（锚点） | 修复前后 P(0) 完全相等 | 487.3 = 487.3 W | ✓ |
| 谷底存在与位置 | v\* 严格内点，10 kg X8 配 40 寸桨合理域约 4–8 m/s | v\*=5 m/s | ✓ |
| 谷底深度 | 实用多旋翼巡航比悬停省 10–30% | 26.1%（360.0 W） | ✓ |
| 左支存在 | P(2)<P(0)、P(4)<P(2) | 440<487.3、381<440 | ✓ |
| 右支上升 | 废阻主导，P(8)>P(v\*) | 410 W > 360 W | ✓ |
| 单调修正量 | 每桨高速节省上限 ≈ T·vi0 ≈ 30 W（全机 ≈ 240 W），无负功率 | max ≈ 27 W/桨（v=14），功率恒正 | ✓ |
| 敏感性带 | gain ±30% → v\* 移动 ±1 m/s 量级、降幅 17–36% | v\*∈[4,6]，降幅 16.7–35.5% | ✓ |
| 无回归 | 其余 22 门 + P0-P4 + 适配器全绿 | 23/23 + PASS + PASS | ✓ |

## 边界

- H3 等级仍为**缺省 + 敏感性**（literature/model）：动量理论无桨尖损失修正，输入为公开物理（桨径/空气密度/推力），不涉内部资料；前飞台架/风洞数据到位后升级；
- T2 正式跑批解冻，但 WP6 前置（A3 门槛 §9.4 重预登记、E_pred/场景预登记冻结入库）仍须先做；
- 降幅 26.1% 与 v\*=5 依赖 H4（CdA=0.3）与 H5（δ=0.111）缺省——三者联合敏感性属 T2 图集报告项。
