# 2026-09-02 M2 第七轮发现修复（R7-F1）

项目组：周航正、霍奕茗、于跃、叶安、王健祺

文件负责人：叶安（平台线）

本次贡献：ZCode（R7-F1 修复实现、崩溃重试语义、探针与击杀模拟、正式批次）

审核：待项目组审核、待指导教师确认

AI协助：ZCode（修复实现与文档整理）

## 本次做了什么

- 评估第七轮独立验收报告（Codex，`bb3097a`）：11/42 事实全部核实，结论认可；补充关键取证——`ff34-25df-6880-531c.dmr`（562 MB，18:26）证明 c5 未落盘是第二条链完成后、阶段盖章前的 MATLAB 原生崩溃，而非验证器代码分支问题。
- R7-F1 修复（`35436ec`）：段入口 `<stage>.attempts` 持久计数器 + `done.attempts` 盖章 + report 逐段打印/归档；c5 链后 `bdclose('all')` 释放 Simulink 堆；批次驱动按规则 v1.4 §2 规则 7 实现有界（3 次）完整重执行重试。
- 规则 v1.4（`fd4ce7c`）：段进程崩溃重试语义（非零退出且无 done 标记才可重试、完整重执行、上界、诚实记账、盖章后退出崩溃无害）。
- 无仿真探针 6 项全过，含两次真实进程击杀模拟（标记 15 s 落盘、崩溃后存活、重试递增 1→2、无假证据）与脏树顺序钉（被拒绝的段入口不写标记）。
- 清理 Codex 第七轮遗留的挂死 MATLAB init 进程（18:07 启动、写完 manifest 后未退出，两小时僵尸）。
- 正式批次（驱动在仓库外 `Temp/run_r7_batch.sh`）：init → c1c2stale → c2clean → c3 → c5 → contract → report，42/42 PASS（数据见证据文档 §4）。

## 关键决策与理由

- 修复定性为"受控韧性"而非"消灭崩溃"：崩溃点随机（4/8/45 次仿真深度、链后落盘前、进程退出时），代码逻辑无法免疫；试图消灭只会引入更大的断言复杂度。
- 计数器放在段入口、源码绑定门之后：被门拒绝的调用不是真实段执行，不计数；通过门的每次入口都计数——包括被击杀/崩溃的尝试，这正是记账的目的。
- 重试从段入口完整重执行，不做任何部分状态续传：时间戳归档天然杜绝新旧状态混用，与单提交批次绑定语义一致。
- 击杀模拟用 taskkill /F 模拟原生崩溃：对计数器语义等价（标记在任何仿真之前落盘）。

## 遗留问题 / 风险

- 全量 9 场景同会话双链仍为环境限制未覆盖组合（R6 口径不变）。
- 本批次各段第 1 次尝试即完成，正式批次未实际触发重试路径（其语义由击杀模拟动态证明）；未来批次若发生真实崩溃重试，证据必须报告实际尝试数。
- 本报告不外推真实八旋翼节能或飞行安全结论。

## 验收状态

- 仓库治理检查：PASS（12 模块 / 24 Markdown，PowerShell 5.1）。
- 本批次 42/42 针对性矩阵 PASS @ `fd4ce7c`；M2 核心继续放行；验收自动化在"当前提交已取得完整矩阵证据"意义上恢复。
