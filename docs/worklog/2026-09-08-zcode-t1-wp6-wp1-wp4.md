# 2026-09-08 T1 执行日：WP6（M3 F4 修复自验）+ WP1（Plane 适配器）+ WP4（台架清洗与初版拟合）

执行：ZCode（平台线，按 [P1_T1_WORK_PLAN](../P1_T1_WORK_PLAN.md) 开工；文件负责人叶安）
冻结提交链：`06a76f6` → `1398f29` → `e3eec09` → `03719ed`（WP6）+ `34569e5`（WP4）+ `96019f3`（WP1）

## WP6：M3 F4 修复（第三轮 Codex 报告 §3/§7）

- 只修四个已实证范围：§3.1 严格身份（live HEAD == 批次提交 + 含 aggregator 在内的 7 指纹现场重算断言，`m3_live_fingerprints.m` 单一指纹源）；§3.2 正式包装入口 done/result/marker 三方记账（done.attempts 六类 → `air:M3BatchAgg:StampAttempts` 等）；§3.3 配置哈希三方链（文件↔result.cfgSha↔done，trials 端写入 cfgSha）+ **14 臂生产路径复核**（归档日志+归档配置重跑 `m3_eval_arm`，判定字段须与摘要精确一致）；§3.4 验证器入口合同门先于预算（vreport 也拦）+ 验证器自身绑定入盖章 + vreport 记账审计。
- 验证（干净树 `03719ed`，dev staged 副本 `results/air_m3_batch_staged/dev_round4c`）：内层 25/25 精确 ID + 正控 2（原样 + 三段替代布局）；正式入口 12/12（done 六类/聚合器 hash 两类/配置浅+深/单臂失败/验证器超上限——超上限探针无盖章、无 attempt 消耗）；真实批次聚合 PASS（14 臂复核 replayDiff=0，门槛值与第三轮报告一致）；vunit PASS；vreport 审计**拒绝不完整链**（vnegative 无盖章，符合设计）。
- 证据：[M3_F4_FIX_20260908](../evidence/M3_F4_FIX_20260908.md)。
- 边界：零仿真；夹具身份为合成现役身份（restampFixture，身份类负向在重 stamp 之上施加）；vnegative + 正式 14 臂批次 + 四态恢复矩阵按 §7.3 待做，M3 仍不放行。

### 过程教训（如实记账）

1. **套件运行期间绝不写仓库文件**：两次 DirtyTree 失败（25 个负向跑到第 8/23 个时被并行写入的新文件打断）均为我并行改文件所致，聚合器行为完全正确。流程修正：后台验收套件运行 = 工作树冻结。
2. `struct('row', {plan(k,:)})` 的 cell 包裹语义：cfg.row **就是**那条 1×7 计划行，多剥一层 `{1}` 会崩（`1398f29`）。
3. dev scratch 副本的三次过程失败如实消耗 attempt、不重置（unwrap 崩溃、DirtyTree×1、夹具 archiveDir 不一致）。

## WP1/T1.2：harness↔Plane 对接

- `harness/+harness/make_plane_adapter.m`：把 P0–P4 plane 对象包装成 harness 飞机接口。语义 = **每个评估点把飞机稳定到该速度再读功率**（策略开环给 v_ref ZOH，平台内部闭环速度跟踪——正是 T1 口径）；导出 schema 0.3 逐查询记录供验收指标表；评价侧真值曲线/最优点只进评价日志（红线 1）。
- `harness/run_plane_adapter_check.m`（`run_harness('adapter')`）5 门全过：稳态点保真 ≤0.009%（4/6.3/9/12 m/s）；小阶跃 8→9 t63=**1.000 s**（τ=1 s 精确呈现；大阶跃由 2 m/s² 限幅主导——与 06 v1.1 §2 决策 4 分层一致，t63 门只在小阶跃有效）；越界钳位 25→20；schema 0.3 一致性；无隐藏随机源（重查差 0.005%）。task2 demo/tests 未动。
- 下一步（WP1 剩余）：nominal_sched/fixed 两臂经适配器的开环批量驱动 + Percent 两臂（WP3）+ 场景预登记清单。

## WP4：电机台架清洗 + 初版拟合（电芯 PDF 未取数）

- `models/plane/data/clean_bench_motor.py`（哈希强校验）：**129 个有效点**，5 个电压块（18/21/24/27/30 V，34/29/25/22/19 行）；**块 #6（#6 号测试，30V，296 行）整块为怠速重复记录 + 尾部纯序号行，无有效数据——06 方案"444 行"是工作表行数，有效数据点是 129**；18V/21V 各 1 行台架复测点（读数差 ~0.1%）保留并打标，单调性在去重序列上验证；P=V·I 偏差 0.00%；原始数据与派生 tidy CSV 均留仓库外（`参考数据\derived\`），清洗记录+哈希进 `processed/`。
- `models/plane/data/fit_bench_motor.py`：分块 P(RPM)/拉力(RPM)/扭矩(RPM) 多项式拟合 + 确定性每第 4 行留出：P 留出 mean 1.2–2.2%/max ≤9.1%，T 留出 mean ≤0.9%/max ≤3.1%；等级声明 **calibrated 限台架静态域**（H3 前进比/H5 共轴不在数据内）。
- **电芯取数单已按硬规则提出**：[CELL_EXTRACTION_SHEET_20260908](../CELL_EXTRACTION_SHEET_20260908.md)——拟取数 7 项（容量 24.0 Ah@0.2C、满充 4.25V、测试放电终止 2.3V、推荐窗口 2.8–4.2V 等，均带页码）；**缺失项：OCV(SOC) 表格数据无（仅附录图像）、直流内阻无测试项、窗口内可用容量未给出**；4 个口径问题待叶安拍板，确认前不取数、不进代码。

## 下一步

1. §7.3 正式复验：干净冻结提交 + 入库驱动跑正式 14 臂（5 段 MATLAB 进程，堆纪律）+ vnegative + 四态恢复矩阵 + 本轮受影响回归；
2. ~~电芯取数单交叶安~~ → **叶安 2026-09-08 已拍板**（"大致物理表现"口径），电池模型 v1 已入库（见下）；
3. WP1 剩余：两臂开环批量驱动 + WP3 Percent + WP5 图集脚本 v1（按已确认样张）。

## 补记（同日晚）：电池建模 v1（叶安拍板"大致物理表现"口径后）

- **拍板**：叶安确认电池按附录实验图线拟合光滑曲线模拟、只求大致物理表现不求精确（取数单 §4 已回填）。
- **附录图线数字化**（Windows OCR 轴标定 + 彩色曲线像素追踪，全部脚本化仓库外）：y 轴标定自洽（0.2V/72px，残差≤5mV）；**x 轴刻度竖排数字 OCR 失败**，用 0–25Ah 等距假设 + 物理闭合检验（2C 终点 24.3Ah ≈ 报告 2C 容量 23.4Ah）；0.2C/2C 充电曲线低电压段提取可信（±0.03V），中高 SOC 段受**图例遮挡**与**竖排数据标注**污染（跳变 ±0.1–0.5V），且两图曲线均未画到满充终点。
- **结论与产出**：数字化不足以做 OCV 权威数据 → OCV 主锚 = 文献 S 形 + 报告窗口锚点（2.80/3.50/4.20 V @ 0/50/100%），数字化结果降级为 R_int 粗估（12 mΩ，0.2C vs 2C 同 SOC 压差中位 0.51V/43.2A）与形状核验。入库 `calibration/battery_model_v1.{json,npz}` + `battery_cell_v1.md`（含 7S4P 组级换算：96 Ah、19.6–29.4 V、3 mΩ）。全部按"大致物理表现"等级声明——支撑定性表现（平台形状/SOC 滑移/压降/截止触发），不支撑电芯级精度。
- 教训：报告附录为**充电曲线**（电压随容量/时间上升），非放电曲线——判读时先确认曲线方向再标定；图内图例框不透明白底会遮挡曲线段，竖排数据标注会被误认为曲线段（宽度判别：曲线段 ≥12px，文字簇 3–6px）。
