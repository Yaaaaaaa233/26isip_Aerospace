# Worklog：M2 复验问题修复与 3 会话复验（ZCode，2026-09-01）

项目组：周航正、霍奕茗、于跃、叶安、王健祺
记录人：叶安（委托执行）、ZCode（执行与成文）
性质：只增不改的执行记录；对应复验报告 `docs/evidence/PROJECT_REACCEPT_CODEX_20260901.md`（F1--F6 / Z1--Z7）

## 执行摘要

1. **F1 根因诊断**：逐样本对比两次九场景归档（`20260901_210517` vs `20260901_220344`，同一磁盘模型；`490711c` 未触碰模型）。发现 fixed 臂同样漂移（Pe ±0.003 W 起于 t≈4.55 s），首个差异样本的八路 PWM 从全 1500 变为 `[1501,1501,1499,1499,…]`——姿态差动指令骑在 pwm=1500.5 的 uint16 舍入边界上，会话级 ulp 浮点差（Normal 模式解释/JIT 会话态，~1e-13）决定舍入方向，闭环放大为 ΔE% ±0.015pp 抖动。S3 的真问题是协议：[20,30] s 窗混入从 1.2 出发的收敛过程惩罚（中心走完 ~0.2 行程需 ~100 s），裕量 0.0075pp < 抖动 0.015pp。
2. **Z4（预注册）**：名义配对组 30→120 s，门槛窗移至收敛末窗 [90,120] s，±0.5% 门槛不变；修订先写入接口文档 §7 再重跑；[20,30] 连续性值保留报告；每次运行前显式 `M2_ETA_APPLIED = center0`。
3. **Z1/Z3**：单元测试 onCleanup 快照/恢复全局 + `clear m2_eta_esc` 清持久态；U1 改为经模型一致的 uint16 量化后再验每对 Σc。
4. **Z2**：试验脚本硬门槛覆盖文档全部条件（带内/无 fallback/无饱和/yaw≤1.5/etaTrk≤0.02 全场景；名义加硬标志静默/接管率/PWM 轨距；esc 名义加收敛非 NaN）。
5. **Z5**：3 个全新 MATLAB 会话按 AGENTS.md 顺序完整链（单元→比较→注入→试验），无人工 clear。结果回填证据文档 §4。
6. **Z6**：`run_unified_acceptance` 未全过时 `error('usearch:AcceptanceFailed')` 并携带 `passed` 字段。
7. **Z7**：AGENTS.md 增 unified_search 模块概要与验收入口；模块 README 增署名占位（正式署名留模块负责人，AUTHORSHIP 规则 5）；本 worklog 即流程补齐的一部分。

## 教训

- "PASS" 的裕量必须与已知环境抖动同报：原证据 S3 +0.4925% 离门槛 0.0075pp，而会话抖动就有 ±0.015pp——这类"骑线通过"应当在验收时就标出，而不是等复验发现。
- 验收脚本与文档判据一一对应（F4）：脚本算了的指标不进 PASS 等于没验收。
- 全局态是会话间的隐形总线（F2）：测试改全局必须自带恢复，执行顺序不应成为正确性前提。

## 状态

修复轮完成并关闭复验清单 F1--F6：

- **Z5 结果（3 个全新会话，无人工清理）**：单元测试 PASS ×3；旁路比较差 0 ×3（紧跟单元测试，Z1 隔离生效）；安全注入 4/4 ×3；九场景试验 PASS ×3。
- **门槛窗 [90,120] s 配对 ΔE%**：S1 −0.25985% / S2 −0.29211% / S3 −0.22617%（三会话逐位一致，最差值 −0.22617%，裕量 0.73pp）；[20,30] 连续性值 +0.35336% / −0.29302% / +0.50619% 与复验报告逐位一致；esc 中心 120 s 收敛到 1.005 / 0.9998 / 1.0302。
- 复验报告 §7 的限制解除：M2 恢复放行（快照 `air_m2.slx` 未变，协议修订已预注册于接口文档 §7），M3 可启动。
- 会话链入口固化为 `models/px4_x8/run_m2_session_chain.m`。
- 详见 `docs/evidence/M2_REACCEPT_FIX_20260901.md`。
