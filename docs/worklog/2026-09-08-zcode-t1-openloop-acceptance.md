# 2026-09-08 T1 开环验收执行：WP1 两臂批量驱动 + WP3 Percent 两臂 + WP5 图集 v1

执行：ZCode（平台线，按 [P1_T1_WORK_PLAN](../P1_T1_WORK_PLAN.md)；文件负责人叶安）
提交：本文件随代码与证据一并入库；运行批次 `results/t1_openloop/20260907_173218_t1`，权威证据副本 `docs/evidence/T1_OPENLOOP_20260908/`。

## 交付

1. **`models/plane/t1_openloop_run.m`** —— T1 开环单次运行器，驱动统一 Plane 代理（P0–P4 契约对象，`plane.step` dt=0.01）。三种模式：`plan`（名义调度 rollout，固定点迭代含跟踪器暂态，产出两臂共用的 E_pred 分母）、`fixed`（常值地速开环臂）、`nominal`（保持空速恒等于代理最优空速 v*≈5.023 m/s 的风补偿调度）。含测量链（0.2 s 延迟 FIFO + 电流 1 A/电压 0.1 V 量化 + 0.3 A 噪声）、8 位 flags（bit0 夹断按指令周期锁存 / bit2 异常 / bit6 缺失 / bit7 截止锁存）、电池窗口占位参数（29.4/19.75/19.6 V、3 mΩ、20 Ah×capScale，仅对齐 7S 标定窗口；完整 OCV 表接入属 P2）。
2. **`models/plane/t1_acceptance_run.m`** —— T1 验收入口：主任务两臂 ×20 种子（T=400 s）+ 六个注入场景（A3 阶跃/夹断、C3/C4、B4 异风、D2 截止），导出 A/B/C/D+S 五组图 + 指标表.md + result.mat。
3. **验收结果：21/21 机器门槛 PASS**（B2 风三角为人工核对项）。核心数：S1 nominal |Percent| 中位数 **0.133%**（参考带 ≤5%）；fixed 中位数 **+11.679%**（不适应代价，仅报告）；S2 配对差 mean +11.509% / max +12.090% / std 0.377%；A3 小阶跃 t63=0.990 s；C3 测量延迟 0.200 s；D1 能量对账 0；B4 异风轨迹差 0。
4. **场景预登记已固化**在两个入口的头注与指标表口径栏（T=400 主任务——保证 ≥2 个摆动周期；电池窗口占位参数；20 种子风幅抖动 N(1,0.05)；"电厂功率偏置"扰动类在代理世界不适用——plant==名义模型，P2 物理化后引入）。

## 调试过程（如实记录，六轮迭代）

| 轮 | 失败 | 根因 | 修复 |
|---|---|---|---|
| 1 | D2 断言"截止后 P>0" | plant 在截止当拍仍完成功率结算，下一拍才为 0——运行器锁存检查 off-by-one | cutArmed 次拍武装 |
| 2 | A2=628 m | plane 的 φ 每 2π 回卷，里程恒等式被回卷跳变污染 | 记录未回卷相位 phiU |
| 2 | B1=2.93 | 恒等式是**切向**口径（w_t+v_air−v=0），我误用 2D 矢量模长把法向风分量算入 | 改切向投影 |
| 2 | B3=NaN | v̄≈5 → 周期 126 s，T=200 只有 1 个峰 | 主任务延长 T=400（预登记更新） |
| 2 | C2=0 | ZOH 检查用在没有指令跳变的 cx 注入运行上 | 换 nominal 主任务 |
| 2 | C3=0 | 异常 ×1.3 施加在延迟之后（测量侧）→ 边沿不延迟；且阈值基线用真值 | 异常移到入队侧（plant 侧语义）+ 阈值基线改 Pmeas 自身沿前后电平 |
| 3-4 | S1=100.18% | E_pred rollout 仍是默认 T=200，两臂已跑 400——分母/分子时长不一致 | plan 调用传 T（注意第三参是 plan 占位，T 传 opts） |

流程教训：本轮全部在**前台短迭代**完成（单轮 ~2 min），未复现 WP6 当天"后台套件 + 并行写树"的 DirtyTree 冲突；MATLAB 参数按位置传递时占位参数（`plan`）易吞掉 opts——调用点显式传 `[]`。

## 与样张的差异说明

正式运行相对确认样张（demo）的三处登记差异：① 主任务 T 200→400 s（B3 需要 ≥2 周期）；② 电池参数从 demo 的玩具值改为对齐 7S 标定窗口的占位参数（容量按任务缩放，P2 接入完整 OCV 表）；③ 每种子扰动去掉"电厂功率偏置 N(1,0.012)"（代理世界 plant==名义模型，该类无从注入，改由 P2 引入）——均在指标表附注与场景预登记中声明。

## 下一步

1. 叶安人工核对 B2 风三角（`docs/evidence/T1_OPENLOOP_20260908/B_风的影响.png` 第二格）后 T1 验收关闭；
2. §7.3 M3 正式批次复验（含 240 s 仿真，单独批次安排）；
3. P2 准备：飞控内核 + 每电机功率合成 + 电池 OCV 表接入（`battery_model_v1`）。

## 验收关闭（同日）

叶安人工核对 B2 风三角通过（"没问题"）→ **T1 开环验收关闭**：21/21 机器门槛 PASS + 1/1 人工核对项。已登记于 `docs/evidence/T1_OPENLOOP_20260908/README.md`。下一步：§7.3 M3 正式批次复验（M3 放行前置）与 P2/T2 准备。
