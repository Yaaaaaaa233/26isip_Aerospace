# 2026-09-04 Codex：M3 第一轮独立验收

项目组：周航正、霍奕茗、于跃、叶安、王健祺
文件负责人：叶安（平台线）
主要撰写：待叶安确认（初稿由 Codex 辅助整理）
技术贡献：叶安（提出按文档验收 M3 的要求）
审核：待叶安与项目组审核
AI协助：Codex（源码与合同核对、MATLAB/Simulink 实跑、归档重算、隔离负向探针、报告整理）

## 本次做了什么

- 在干净提交 `6634c5b62e374573e3dd8f8cf4d24d79e1d33e91`，按 M3 v0.2 / 验收规则 v1.7 固定本轮范围。
- 形成 [独立验收报告](../evidence/M3_REACCEPT_CODEX_20260904.md)，含四层判定、6 项新发现、原设计问题映射、实际矩阵、attempts、裕量和未覆盖项。
- 实跑 M3/M0-C/M2 单测；8 次 Simulink 仿真（M0-A 旁路 2、M0-B 故障注入 4、M3 160 s 边界 2）均通过各自已测门槛。
- 读取历史 14 个场景（原报告误写 13），重算 8 个能量配对、重放 5 个名义臂 eta 真中心、核验历史 N5/R1；中心重放候选逐样本差 0，历史收敛/单调性有正向证据。
- 执行 7 个独立负向样例，确认收敛方向错误、正式单调性未硬判、停搜/NaN/扰动长饱和误放行；原始数据未改，生产评价器只复制到 ignored 诊断目录中调用。

## 判定与原因

- 功能层 PARTIAL（保留已测机制/数值），基础设施 NOT VALIDATED，环境 OPEN LIMITATION，文档 PARTIAL；不支持当前整体放行。
- P1：当前脚本将 B2 也设为 2e-4（历史 B2 正确为 1e-4）；正式收敛判定脱节；执行/稳定漏判；分段绑定/聚合/恢复矩阵缺口。
- P2：有效样本积分被描述为全周期能量（B1 配对实际只用 4.8/96 s）；场景数、边界配置、状态与全局登记不一致。
- 未发现同一冻结合同下 M2 核心功能回归；M3 新验收链没继承旧防线，不等于旧修复失效。

## 环境与修改边界

- 沙箱首启 MATLAB 未进入脚本；核实并清理仅本次测试进程后，沙箱外运行成功。无自然堆损坏；五份原生日志零 U+FFFD。
- 仅新增报告/本简报；诊断文件和结果位于 `results/m3_codex_review_20260904/`，gitignored。不修改源码、模型、原始归档或历史报告；未 commit/push。
- 状态页仍为此前口径，本次不直接改项目组的阶段放行记录；建议负责人根据独立报告同步结果边界。
- 收尾：关键源码/模型指纹不变，tracked 文件无改动；治理检查、两份文档链接/署名/编码检查均通过。

## 下一步与未覆盖

- 先按报告 F1–F4 补配置、单一正式评价路径、负向与分段治理；F5/F6 校正文档，若改正式积分合同先升版本。
- 再跑新干净提交的冻结 E 批次及受影响长回归。每项关闭保留原始复现、针对性负向、既有回归三件套。
- 本轮未新跑 240 s/14 场景 E 批次、完整 M0-C/M1/M2 长回归、跨进程抖动和全部状态×错误出口；Plane/R4 不在本轮范围。
