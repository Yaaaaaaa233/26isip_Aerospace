# 2026-09-02 Codex M2 第五轮独立验收

## 本次做了什么

- 刷新主库到 `cf2fc97`；本轮仅验收叶安负责的 `models/px4_x8` M2 平台线。
- 依仓库说明在独立 MATLAB R2022b GUI 进程中执行 `c1c2stale / c2clean / c3 / c5 / report`，标准针对性矩阵 15/15 PASS。
- C5 同会话双链 2/2 PASS，两份 `summary.csv` SHA-256 相同；S1/S2/S3 为 −0.262619% / −0.293804% / −0.214654%，在 ±0.015 pp 抖动带内。
- 增加非有限调用者状态和 report 新鲜度负向探针，形成 `docs/evidence/M2_REACCEPT_ROUND5_CODEX_20260902.md`。
- 临时 GUI 驱动已删除；未改 `.m` 项目源文件或 `.slx`，运行证据仅留在 gitignored `results/`。

## 关键决策与理由

- M2 核心模型和数值协议保持放行：标准矩阵、完整九场景、双链确定性均独立通过。
- 验收自动化降为 PARTIAL：非有限全局状态不精确恢复，且 `report` 可复用无来源绑定的旧 stage 文件。
- M3 可继续，但上述自动化缺陷须在下一里程碑放行前关闭。

## 遗留问题 / 风险

- R5-F1：四个入口对 `M2_ETA_APPLIED` 的快照都过滤 NaN/Inf，真实错误出口会把原值改为空。
- R5-F2：分段产物没有 run ID/提交/源码指纹，`report` 可能接受旧批次或混批证据。
- R5-F3：trials 归档路径检查只检查字面量 `injected`。
- 本机 R2022b 长序列单进程堆损坏限制继续有效。

## 下一步

- 先用 manifest 绑定分段运行，并修正四入口快照与路径断言；按第五轮报告关闭条件复验。
- 并行推进 M3 方案：两个 ESC 的更新/保持/仲裁、共享稳定窗口与约束总线、同场景单变量基线比较。

## 验收状态

- M2 标准分段矩阵：15/15 PASS。
- M2 核心成果：PASS；验收自动化：PARTIAL。
