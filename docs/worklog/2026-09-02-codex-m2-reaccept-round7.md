# 2026-09-02 M2 第七轮独立验收

项目组：周航正、霍奕茗、于跃、叶安、王健祺

文件负责人：叶安（平台线）

本次贡献：Codex（版本核对、MATLAB 分段复跑、c5 结果与聚合器行为核对）

审核：待项目组审核、待指导教师确认

AI协助：Codex（验收执行与报告整理）

## 本次做了什么

- 核对主库当前提交 `9cf9805`，本地 `main` 与 `origin/main` 一致，工作树干净。
- 在 MATLAB R2022b 分段运行 `init`、`c1c2stale`、`c2clean`、`c3`、`c5`。
- C1/C2 共 9 行与 C3 2 行实际落盘并为 PASS；c5 两条最小链的 `summary.csv` 哈希一致，S1/S2/S3 与登记值一致。
- c5 未生成 `c5.csv`/`c5.done.mat`；运行 `report` 的负向检查确认聚合器拒绝缺段证据。
- 运行 PowerShell 7 与 Windows PowerShell 5.1 仓库治理检查，均 PASS。

## 关键决策与理由

- 将本轮正式矩阵记为 11/42，而不是把 c5 原始归档或历史 42/42 批次替代当前批次的阶段 PASS。
- 保留 M2 核心放行，因为数值门槛有充分裕量，且 c5 两条实际模型链输出确定；仅将验收自动化闭环维持为 PARTIAL。

## 遗留问题 / 风险

- R6-F3 尚未关闭：c5 阶段封装在第二条最小链结束后未落盘，当前批次无法进入 contract/report 正向聚合。
- contract 的四态/篡改负向需在 c5 稳定后随新批次完整重跑。
- 不能把平台仿真代理结果外推为真实八旋翼节能或飞行安全结论。

## 下一步

- 定位 c5 完成后到 stage 写入之间的 MATLAB 退出/异常路径。
- 修复后以当前提交从 `init` 开始完整重跑 42 行针对性矩阵，并记录新的 runId、源码指纹和哈希。

## 验收状态

- 仓库治理检查：通过（12 个模块 / 24 个有效 Markdown）。
- 本轮正式矩阵：11/42 已完成且通过；c5/contract/report 正向闭环未完成。
