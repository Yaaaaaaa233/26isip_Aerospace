# 2026-09-08 数据治理勘误：`raw/功率-升力.xlsx` 前向移除（方案 2）

执行：ZCode（平台线）；拍板：叶安（2026-09-08，在方案 1"确认字段留库使用"/方案 2"前向移除+勘误"/方案 3"保持现状"三选一中选定方案 2）。

## 对象与问题

`models/plane/data/raw/功率-升力.xlsx`（8.7 KB，2026-09-02 提供）：5 组数值点，表头乱码、字段/单位/采样条件未确认，疑似与台架 444 点表同类重叠；属内部参考数据却被跟踪入库（`66a1d5b`），与"内部参考数据禁止上 git"规则冲突。历史改写需 force-push（仓库禁用），故只做前向处置。

## 执行内容

1. 原件字节未动，移出仓库至 `第二阶段\参考数据\功率-升力.xlsx`；SHA-256 `01fd95609f6aa7ca004875c060efa0ddcbebcb25a81ca92b726a07547428b1b3`；
2. `git rm` 前向移除（工作树与索引自本提交起不再含该文件；`raw/` 目录清空，今后仓库内只进清洗记录与哈希）；
3. 勘误登记：`models/plane/data/README.md`（勘误全文 + 使用限制继续有效）；`docs/architecture/06_platform_power_environment_model.md` §4 治理检查点与 §8 登记事项 5 同步关闭；
4. git 历史不改写（禁 force-push）：旧提交中的副本保留，本勘误为完整来龙去脉登记。

## 使用限制（不因移除而失效）

字段、单位、采样条件、传感器与时间基准经数据提供者确认前，不得把该文件（或其副本）接入 `plane.config`、不得标记为 `calibrated`、不得据此生成真实节能结论；若日后确认可用，走正常清洗流程（`processed/` + 哈希 + 清洗记录），原件仍留仓库外。

## 边界

本勘误只涉及数据治理，不涉及任何验收结论；未运行任何仿真，未改动模型代码。
