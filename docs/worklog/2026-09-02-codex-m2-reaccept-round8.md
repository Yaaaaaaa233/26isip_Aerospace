# 2026-09-02 M2 第八轮独立验收

项目组：周航正、霍奕茗、于跃、叶安、王健祺

文件负责人：叶安（平台线）

本次贡献：Codex（独立复跑、负向探针、第八轮报告与状态同步，AI 协助）

审核：待项目组审核、待指导教师确认

AI 协助：Codex

## 本次做了什么

- 在干净 `798f60e` 上复跑 `init -> c1c2stale -> c2clean -> c3 -> c5 -> contract -> report`，42/42 PASS。
- runId `6ade3744-ef6b-449c-9c78-afe81c42fd67`，五个阶段均为 attempt 1；C5 双链哈希一致。
- S1/S2/S3 为 −0.262619% / −0.293804% / −0.214654%，对 +0.5% 门槛最小裕量 0.714654 pp，不骑线。
- 执行 attempt 篡改负向探针：`c5.done.attempts=4`且 marker=1 时 report 仍 42/42 PASS，确认 R8-F1。
- 审计确认有界重试驱动仅位于仓库外临时脚本（R8-F2），attempt 标记为非原子截断写（R8-F3）。
- 负向探针后恢复原始 done 并删除伪造归档；保留正式归档 `results/round4_closure/20260902_210055/`。

## 关键决策与理由

- M2 核心与数值继续放行：当前提交的 42 行功能矩阵和数值门槛均通过。
- 验收自动化保持 PARTIAL：最终聚合尚不能硬保证 attempt 证据合法，仓库也未交付实际重试驱动。

## 遗留问题 / 风险

- R8-F1：聚合器未校验 attempt 字段/范围/marker 一致性。
- R8-F2：有界重试驱动未入库，无法从仓库独立复现。
- R8-F3：attempt 计数写入非原子，崩溃于截断写窗口可使重试链硬失败。
- 全量 9 场景同会话双链仍未覆盖；当前结论仅限 proxy 平台。

## 下一步

- 修复 R8-F1/R8-F2/R8-F3，增加 attempt 篡改负向矩阵和驱动层重试测试。
- 在新 HEAD 上由入库驱动重跑 42 行批次，再决定是否关闭验收自动化 PARTIAL。

## 验收状态

- 仓库治理：PowerShell 7 PASS；Windows PowerShell 5.1 PASS。
- MATLAB R2022b 当前提交针对性矩阵：42/42 PASS。
- attempt 负向篡改探针：FAIL（不合法证据被 report 接受）。
- 综合：M2 核心 PASS；验收自动化 PARTIAL。
