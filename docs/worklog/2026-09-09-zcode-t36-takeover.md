# 2026-09-09 ZCode：3.6 platform_link 接手修复（叶安拍板 D1–D3）

负责人：叶安（平台线）｜AI协助：ZCode｜状态：**完成**（正式批 52/52 + 20/20，平台五门全过）

## 背景与诊断（2026-09-09 上午）

王健祺 2026-09-08 23:03 提交 `d3f7966` 将 3.6 platform_link 以 WIP 移交。叶安查 git 后 ZCode 诊断复现：

- **F1 根因**（P1）：`make_platform_plant` 的 `q()` 调 `adv(stepPerSec)`，参数语义是秒、传入是内环步数（100）→ 单次查询推进 100 s（直接验证：count 0→100）。标定 150 样本需 ≥15000 s ≫ 900 s 预算 → 拟合从未执行，û\* 停在初值 7.5。交接文档三嫌疑（就位语义/预算口径/逐秒交互代价）均非根因；known 劣于 openloop 亦为同一伪影。
- **F2**（P1）：Phase B `wind_corr` 无界累加（拟合器有 ±8 钳位但在线修正路径绕过）→ 一行修复后实测 |ŵ| 发散到 ~32 m/s、717/897 步调度不可行、Phase B 平均地速 0.53 m/s。
- **F3**（P2）：disc<0 兜底 `v=max(q,0)` 不物理。
- **F4**（P2）：平台后端无 rng 种子（本地后端有）→ 不可复现（旧 CSV 14.88% vs 复现 7.68% 即此故）。
- **F5**（P2）：R2022b 上 `exist('pkg.fn','file')` 恒 0（实测 which 可解析仍 0）→ 入口断言必炸；回退路径多一级仓库名。
- **F6**（P3）：旧证据 report 20/20 与代码状态矛盾（F1 下标定不可能完成）——陈旧产物混入。
- **F7**（P3）：两模块未登记（治理 FAIL）。

## 实施（方案 docs/T36_PLATFORM_LINK_TAKEOVER_PLAN.md，阶段 0–3）

- 阶段 0：`7d6a7cb` 方案入库 + 模块登记（治理 PASS 24 模块）。
- 阶段 1：F1/F4/F5 修复落盘 + 新增单测 M1（单次 q() 恰 1.000 s/1 行）与 M5（|ŵ|≤8 全程）。
- 阶段 2：三道守卫（A ±8 钳位 / B 覆盖门 0.8 rad+1.0 m/s / C 可行指令保持）落 `sweepcal_run`，第一轮即过带。
- 过程修正：回退暂存需整拷 `models/plane`（含 data/calibration）；一次手工预暂存残留与一次 repoRoot 级数笔误均由单测暴露后修正（52/52 后才进正式批）。
- 阶段 3：正式批两遍（12:15/12:27）52/52 + 20/20，三 CSV 逐位相同；**如实记录**：第 2 遍首次尝试在单测后遇 MATLAB 原生堆崩溃（0xc0000374，运行时层，与本项目 M3 历史同类），重跑完整通过。

## 结果（平台五门，seed 11 composite 900 s）

û\*=5.0765（标定瞬间 5.6712，真值 5.15）｜尾段 regret 2.98%（超额 8.14%）｜known −0.40%｜purerl_off 4.71% vs 开环 5.61%｜rows=900。D3 条件项（就位语义深化）未触发。证据正本 `docs/evidence/platform_link/`（旧件归档 `*_pretakeover_20260908`），交接文档加关闭段，状态页 16 更。
