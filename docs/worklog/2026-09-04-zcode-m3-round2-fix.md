# 2026-09-04 ZCode：M3 第二轮修复（第一轮独立验收 F1–F6 关闭）

项目组：周航正、霍奕茗、于跃、叶安、王健祺
文件负责人：叶安（平台线）
主要撰写：叶安（修复范围与关闭判据授权）、ZCode（实施记录代拟）
技术贡献：叶安（按第一轮独立验收逐项关闭的决定）；Codex（第一轮独立验收报告）
审核：待叶安与项目组审核
AI协助：ZCode（代码改动、批次执行、负向矩阵、证据整理）

## 本次做了什么

- 按 [`../evidence/M3_REACCEPT_CODEX_20260904.md`](../evidence/M3_REACCEPT_CODEX_20260904.md) §6 顺序修复 F1–F6，全部改动位于验收入口/评价器/检查器/文档，零 `.slx` 变更、ESC 内核与适配器算法未动。
- 新增 `m3_eval_arm`（单一逐臂生产评价路径：收敛/执行检查器/2 s 位 1/2 门接线）、`m3_aggregate_batch`（14 臂 manifest 总聚合：源码绑定、FAIL 行硬断言、R1 同会话/等网格、门槛重算）、`m3_source_binding`（现场 runId/commit/脏树/SHA 取证）。
- `m3_eval_convergence` 参考点修正（abs(pe−1.0)，原距 0 方向反转）；`m3_eval_energy` 双轨字段（覆盖率+全窗）；`m3_check_execution` 重写（有限性、搜索参与度、vViaApplied、etaAppliedIsActual 物理跟踪界、vHoldMask 监督掩码）。
- 入口改造：按臂参数集（m3=2e-4/基线=1e-4）+ 字面冻结表断言；写入后失败钩子（savefail/postwrite，受控注入逃离逐臂降级路径）；R1 缺席硬失败、长度硬断言、vTrk 四对；边界入口声明 1e-4 机制探针口径并归档配置。
- 最终提交 `1e3e0e4` 上完成：五段 14 场景批次（全部 attempt=1）+ 聚合 PASS、8/8 聚合篡改负向、6 生产路径负向 + 正控、11 行恢复矩阵、边界 2×160 s、M0-A/M0-B 回归、三套单测（含新增 B7）。

## 判定与原因

- 第一轮 4 项 P1 全部关闭：F1（配置漂移→按臂分列+冻结断言+错配负向）、F2（评价脱节→单一评价器+单调性硬判+窗口统一 [192,240)）、F3（漏检→checker 接入正式链+全臂稳定门）、F4（治理缺口→绑定+manifest 聚合+写入后恢复矩阵+R1/vTrk 硬门）。
- 批次数值与 2026-09-03 历史批次逐位一致（同冻结配置确定性复现）；对 B1 −0.277~−0.306%、对 B2 |ΔE| ≤ 0.0032%、收敛/单调全过、repro 差 0。
- 环境层维持 OPEN LIMITATION：实施期间 2 次段级堆崩溃（无完成证据，按受控重试完整重执行）；最终批次五段全部一次通过。

## 实施中次生缺陷（如实登记，详见证据报告 §3）

冻结表破坏子集段、受控注入被降级路径吞掉、MAT v7 2GB 静默跳存、struct() cell 复制致 1×N result、checker 对真实通道语义两处误设（el 列 2 为物理实际比值、v 施加通道受监督覆盖）、聚合三次实施缺陷（旧字段引用/cell 比较/verdict 层次）与验证器篡改案例布局依赖。其中多项由本轮自建负向矩阵先行抓出（cfgmismatch 行、ArmFailed 篡改、ReproSession 布局），符合"验证器先于批次"的预期成本结构。

## 环境与修改边界

- 驱动文件置于仓库外（脏树门实测拒收过误入库的临时驱动）；MATLAB 命令串/GBK 控制台解析问题以驱动文件+文件系统真值规避（教训：`-batch` 单行命令对复杂断言不可靠、`run()` 会临时切换目录、printf 转义会破坏 MATLAB 字符串字面量）。
- results/ 全部归档 gitignored 不入库；原始日志在 `results/m3_final_*.log`、`results/m3_round2_*.log`。
- 历史报告与归档原样保留；本轮文档改动：接口 v0.2 勘误块、规则 §7 注册表、状态页、路线图（含 M2 第六轮旧状态段指向第十轮关闭）。

## 下一步与未覆盖

- 待项目组：两轮验收报告的追认；Plane 复跑（R4）待 P0–P4 就绪。
- 未覆盖（登记）：跨机器/跨进程抖动、全笛卡尔状态×出口矩阵（本轮为针对性矩阵逐行列明）、M3 专用跨进程崩溃重试驱动（现为一次性时间戳段+受控重试+如实记账）、V1 日志限制（sat 不入 v 在线门、位 5 离线、v 候选/eta 施加参考不落日志）。
