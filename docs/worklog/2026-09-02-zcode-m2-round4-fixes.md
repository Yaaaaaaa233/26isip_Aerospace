# Worklog：M2 第四轮复验修复——总验证器自身合规（ZCode，2026-09-02）

项目组：周航正、霍奕茗、于跃、叶安、王健祺
记录人：叶安（委托执行）、ZCode（执行与成文）
性质：只增不改的执行记录；对应第四轮复验报告 `docs/evidence/M2_REACCEPT_ROUND4_CODEX_20260902.md`（R4-F1/F2/F3 与 §6 关闭条件）

## 背景

第四轮复验确认三轮核心缺陷全部关闭（含 Codex 自做的 persistent 动态直探 maxDiffFresh=0），M2 保持放行；新发现全部指向我在第三轮修复中新建的总验证器 `verify_m2_round4_closure` 自身违反我立的规则——执行契约的工具自己不守契约。

## 修复

1. **R4-F1（高）验证器不恢复调用者全局**：入口快照两全局 + 函数帧 onCleanup 恢复（成功/异常）；删除原"末尾清空"及误导注释；新增 `'fail'` 自注入钩子（快照与全局触碰之后、矩阵之前触发 `air:M2Verify:InjectedFailure`）。
2. **R4-F2（中）persistent fresh 仅间接推断**：把直探固化进 C1b。实现中发现 Codex 报告与我初版的探针（t=0 首次调用对比）都是**空洞的**——t=0 触发时间回退重初始化，清没清输出都一样；改为**前向时间探针**：warm 用 gain=0.5 驱动中心显著漂移，错误后在 t=1.05（warm 末端之后）调用——清理生效则重初始化（复位参考），漏清则延续漂移态；另加**探针灵敏度自检**（故意不清的 drifted 调用必须 ≠ fresh，实测差 0.0485），防探针本身退化成永真。
3. **R4-F3（中）"全矩阵"措辞强于实际覆盖**：双管齐下——矩阵补 clean 入口态 4 行（链三类错误出口与单元错误出口各一）；所有措辞收窄为"**针对性矩阵**"（15 行，逐行列 case，未覆盖组合明确列名）；`ACCEPTANCE_AUTOMATION_RULES.md` 升 v1.1：§4.3 澄清矩阵范围=缺陷类别相关的全部组合、声明行数=实际行数、未覆盖全部组合时必须命名"针对性矩阵"；§6 自检清单加"验证器/验收链自身状态契约须直接动态证据"。

## 过程失误（如实记录）

- 重写验证器时 clean 变体的 `checkRestored` 少传一个参数，首次驱动运行即报错；顺带发现空期望值需要专门分支。
- C1b 直探初版照搬"首次输出对比"思路，未察觉 t=0 重初始化使探针永真——这次是我自己先抓住并改为前向时间探针 + 灵敏度自检，而非等复验指出。

## 验证

**结果（分段编排，2026-09-02 12:10）**：`fail` 注入 → 捕获 `air:M2Verify:InjectedFailure` 且调用者状态恢复（COND1A）；c1c2stale（6 项）、c2clean（3 项）、c3（2 项）、c5（4 项）各段 PASS 且**每段**非空调用者正常返回后逐域恢复（COND1B ×4）；report 聚合 15/15 PASS（归档 `results/round4_closure/20260902_121040`，双链哈希互等 `f5fcb798…`，门槛值 −0.2626/−0.2938/−0.2147% 在 ±0.015pp 内）。

**环境事件（如实记录）**：当天本机 R2022b 出现多次**进程级堆损坏**（0xc0000374），崩溃深度 4/8/45 次重仿真不等、与并发 GUI 会话无关、磁盘内存充足——判定为环境抖动；对策为验证器分段执行（每段独立进程 ≤30 次仿真；C5 双链保持同段进程以满足同会话哈希口径）。c3 第一次尝试的全部检查通过后崩溃于 MATLAB 退出阶段（工作已完成，rc=127），重试干净通过。驱动侧另修两处：readtable 未指定 Delimiter 把空格当分隔符；行数断言误用 numel（数到 15×4 单元格元素）。

R4-F1/F2/F3 关闭，第四轮 §6 五条关闭条件全部满足；M2 放行，进入 M3。详见 `docs/evidence/M2_REACCEPT_ROUND4_FIX_20260902.md`。
