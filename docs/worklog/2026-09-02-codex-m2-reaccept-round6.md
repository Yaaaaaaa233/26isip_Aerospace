# 2026-09-02 M2 第六轮独立验收与状态修订

项目组：周航正、霍奕茗、于跃、叶安、王健祺

文件负责人：叶安

本次贡献：Codex（主库与版本核对、MATLAB/Simulink 分段复验、四态契约探针、验证器来源与聚合审计、验收报告及状态文档代拟）

审核：待项目组审核、待指导教师确认

AI 协助：Codex（验收执行、证据审计与文档整理）

## 本次做了什么

- 在 MATLAB R2022b 中对 `38ce63b` 的 M2 第五轮修复执行第六轮独立验收；报告编写时主库为 `f75b77e`，其间没有 M2 源码变化。
- 标准分段完成 `c1c2stale` 6/6、`c2clean` 3/3、`c3` 2/2；C5 两次均完成第一条链、在第二条链中途退出，故标准矩阵按 11/39 记录。
- 单独运行有限/空/NaN/Inf 四态契约探针，四入口共 20/20 PASS，确认 R5-F1 关闭。
- 审计 manifest 与聚合器，发现 stage 未独立绑定真实 HEAD/验证器哈希/dirty 状态，以及聚合器未断言 verdict 全 PASS。
- 新增第六轮证据报告并同步当前状态口径；未修改模型、算法或验证器源码。

## 关键决策与理由

- M2 核心数值协议继续放行：九场景数值稳定且最小门槛裕量 0.714654 pp，明显高于 `±0.015 pp` 抖动。
- 不把独立 20 项契约探针与 11 项标准分段相加冒充 39/39；C5、正式 contract 和 report 未完成时，总验证器只能判 PARTIAL。
- 修订第五轮“manifest 已绑定当前提交”的强结论：当前实现绑定的是 init 声明字段，不是每段真实执行源码。

## 遗留问题 / 风险

- R6-F1：stage/report 需重新读取 HEAD、验证器 SHA 与 dirty 状态，拒绝 `unknown`、dirty 和来源漂移。
- R6-F2：聚合器需硬断言全部 verdict 为 PASS，并补 FAIL 行负向证明。
- R6-F3：R2022b 长序列 C5 在第二链重复退出，完整 39 行独立闭环尚未取得。
- R5-F3 的代码已收紧，但缺路径负向证明未在本轮正式 contract 段复跑。

## 下一步

- 由实现方按第六轮报告 §8 修复验证器，再在可稳定完成 C5 的环境或精简链上重跑正式 39/39。
- M3 方案与调度器可继续；下一平台里程碑不得引用当前总验证器为“完全闭环”。

## 验收状态

- 标准矩阵：11/39 已完成且通过；C5 两次长序列退出，正式 contract/report 未运行。
- 独立四态契约探针：20/20 PASS。
- M2 核心/数值：PASS，保持放行。
- 验收自动化：PARTIAL。
- 仓库治理检查：PowerShell 7 运行 PASS（12 个登记模块、24 个活动 Markdown）；Windows PowerShell 5.1 因治理脚本 UTF-8 中文字符串解码错误无法解析，属工具兼容性问题，未在本轮修改。
