# T2 场景预登记（正式跑批前冻结）

版本：v1.0（冻结版）
日期：2026-09-08
状态：**冻结**——按 [`ACCEPTANCE_AUTOMATION_RULES.md`](ACCEPTANCE_AUTOMATION_RULES.md) §9.4 与 [T2 验收标准清单](T2_ACCEPTANCE_CHECKLIST.md) v1.2 §3（A3/A4 重预登记）冻结于 T2 正式跑批之前；跑批后不得修改，改动即升版重跑。
依据模型：P2 物理链（`models/plane/+plane` @ 提交 `f55c458` 系：俯仰内核 H6 / 台架合成 / H3 动量理论修正 / H9 上限 k_n=38.8 / bit4 / battery_model_v1）。被测入口 `models/plane/t2_openloop_run.m` / `t2_acceptance_run.m`（本登记的头注与本文件一致性由 v\* 断言保证）。

## 1 主任务（Percent 两臂）

| 项 | 冻结值 |
|---|---|
| 时长 T | 400 s（≥2 个完整摆动周期：2πR/v̄ ≈ 2π·100/5 ≈ 126 s） |
| 步长 dt / 指令周期 Tc | 0.01 s / 0.5 s（ZOH） |
| 轨迹 | 圆 R=100 m，圆心原点 |
| 风 | 恒定世界风正东 W0=3 m/s（不进力平衡，唯一路径 w_t→v_air）；每种子幅抖动 W=W0·N(1,0.05) |
| 种子 | 1..20（seedBase=4200；测量链独立噪声流 seedBase+seed+5000） |
| 两臂 | fixed：v_ref=8 m/s 常值；nominal_sched：v_ref(t)=clip(v\*+w_t(φ),0,20) |
| v\*（名义调度锚点） | **5.0 m/s**（P2 解析名义图数值极小点，0.05 m/s 网格；运行时断言偏离 ≤0.25 m/s） |
| 电池 | P2 物理链默认（7S4P 96 Ah，OCV 表+内阻 0.021 Ω），**无任何占位/缩放覆盖**；soc0=1.0 |
| E_pred 分母 | 名义图 P_nom(v_air)（解析稳态：废阻前馈俯仰 → m·g/cosθ/8 → 台架 T(n) 反解 → P(n;Vfull) → H3 节省 → 共轴 δ → +废阻+辅助；**满 OCV 电压口径冻结**）沿 4-迭代固定点 rollout 调度（含暂态）积分；每种子一份，两臂共用 |

## 2 注入/专项场景（全部预登记）

| 场景 | 参数 | 测什么 |
|---|---|---|
| A3 小阶跃 | T=60，W=0，fixed，seed1，vStep=[10, 8, 9] | t63（俯仰内核动态）与 max\|dv/dt\| |
| A3 大阶跃 | T=60，W=0，fixed，seed1，vStep=[10, 0, 8] | 指令级限幅主导段 max\|dv/dt\| ≤ 2 |
| A3 夹断 | T=60，W=0，fixed，seed1，inject clamp [30,50)（v_cmd=25） | applied=20 + bit0 窗内/窗外 |
| C3/C4 | T=200，fixed，seed1，anomaly [60,80) + invalid [100,110) | 测量延迟、bit2/bit6 |
| B4 异风 | T=60，fixed，seed7，W=0 vs W=3（幅抖动关） | 轨迹逐点差 = 0 |
| D2 截止 | T=200，fixed，seed1，**soc0=0.05**（初始深放电，无容量缩放） | 截止锁存、SOC 单调 |
| E 标准饱和负向 | T=60，W=0，fixed，seed1，v_ref=12 + **az_cmd_mps2=2.0**（爬升需求），满电 soc0=1.0 vs 亏电 **soc0=0.15** | 分层：满电臂零饱和；亏电臂 bit4、bit0 恒不亮、ΣT 精确封顶 |

## 3 门槛（重预登记，§9.4 留痕）

| # | 指标 | 冻结门槛 | 依据 |
|---|---|---|---|
| A3 小阶跃 t63 | **3.2–3.8 s** | 干跑实测 3.480 s；理论主极点 1/kp=2.857 s + 俯仰滞后 0.3 s + ZOH 0.5 s 复合（旧 T1 门 0.9–1.1 为 τ=1 代理口径，随内核替换作废） |
| A3 max\|dv/dt\|（小/大阶跃） | ≤ 2.0 m/s² | 指令级限幅（H6 分层的声明值；大阶跃由其主导） |
| A4 静默窗跟踪误差 | **≤ 0.5 m/s** | 干跑实测 0.330 s；内核带宽 kp=0.35 s⁻¹ 下摆动转折邻域残差（旧门 0.2 为 τ=1 口径，作废） |
| D4 电压窗 | 量化 Vb ∈ [19.6, 29.4]（D2 场景取截止前样本；量化分辨率 0.1 V） | 干跑实测 D2 截止前 Vmin=19.60 |
| E 组四门 | 满电臂 bit4≡0；亏电臂 bit4 步数>0 且 ΣTact−Tce≤1e-9；**全程 bit0≡0**（分层直接证据）；亏电臂不达 12 m/s（shortfall 可见） | 干跑实测：满电 0 步饱和达 12 m/s；亏电 5999/6000 步饱和、封顶残差 0、v_end=0.02 |
| S1 nominal \|Percent\| 中位数 | ≤ 5%（参考带） | 干跑实测 seed1 = −2.11%（图满 OCV 口径 vs plant 电压反馈的物理差，方向与量级已核） |
| S1 fixed Percent | 仅报告（不适应代价） | 干跑实测 seed1 = +16.2%（U 形恢复后深于 T1 的 +11.7%，量级符合谷形） |

其余门槛（A1/A2/B1/B3/B4/C1–C4/D1/D2/D3/S2）沿用 [T1 清单](T1_ACCEPTANCE_CHECKLIST.md) §2 数值不变（B3 摆动周期 ≤5% 等）。

## 4 冻结声明

- 本登记入库后，T2 正式跑批（`t2_acceptance_run`）以本文件为唯一场景与门槛依据；指标表由脚本导出并与本表逐行对照；
- 名义图公式与满 OCV 电压口径随本登记冻结——跑批后调整名义图即构成口径违约（防"看结果调分母"）；
- 电池链无任何演示性缩放（capScale 类手段自 T2 起禁止，深放电用 soc0 初始化实现）。
