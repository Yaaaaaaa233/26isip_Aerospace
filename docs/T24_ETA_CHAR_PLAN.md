# T2.4 方案修订：eta 特性展示（v1.0 生效版）

版本：v1.0
日期：2026-09-08
状态：**已经文件负责人叶安确认（2026-09-08 三项拍板："可以"）**——① δ(v)=δ₀·(vi(v)/vi₀)^κ 形式、κ=0.5 缺省；② 演示用曲线族（含 η\*(v) 漂移）替代单条曲线；③ eta 寻优平台冻结登记。
背景：eta 在线寻优不作为本项目阶段目标（时间约束，2026-09-08 叶安决定）；作为共轴八旋翼项目仍须体现共轴特性，以"给定不同 eta 出模拟结果"的**固定参数对比**形式呈现，寻优列为未来研究方向。

## 1 决策记录

| # | 决策 | 内容 |
|---|---|---|
| D1 | 原 T2.4（M3 十四臂统一 Plane 复跑）**登记性取消** | 其全部机器（内核移植/SNR 可辨识性/配对批）为验证"搜索"服务；寻优取消后无存在理由。M3 在其自身 px4_x8 代理链上保持已放行状态（第四轮独立复验 C1–C7），不受影响 |
| D2 | **eta 寻优平台冻结** | px4_x8 侧 M3 寻优机器（m2_eta_esc mode='m3'、m3_schedule、批次/验证器链）按已放行状态冻结：不移植到 Plane、不扩展、不随本项重开；解冻须项目组新决议 |
| D3 | eta-P 模型**适度增密**（不寻优 → 丰富度约束消失，代价仅为登记假设） | 见 §2；同时删除现模型中把谷底人为钉回 η=1 的回中项——谷底位置改由物理（δ 不对称）自然给出 |

## 2 模型增量（最小参数集，全缺省等级+敏感性带）

| 项 | 形式 | 说明 |
|---|---|---|
| δ 随前进比衰减 | **δ(v_air) = δ₀·(vi(v_air)/vi₀)^κ**；δ₀=0.111（H5 原值），κ 缺省 **0.5**，敏感性带 0.3–1.0（κ=1 纯动量比值、κ=0 退回常数） | vi 为 H3 已有函数（复用，不新造形状）；物理：前飞尾迹偏斜、下桨逃出上桨尾迹 → 干扰衰减 |
| 分配映射 | s_up = 0.5 + **0.55**·(1−η)（原 0.3）；**删除** δ 的人造回中项（原 +0.4·偏移） | 谷底位置成为模型**输出**：悬停 η\*≈0.90（文献锚）、巡航 ≈0.94 向 1 回漂（模型预测，P4 可验证） |
| 旋流回收 | 不单独建模，声明**并入 δ₀ 净口径**（0.85–0.90 区间本即"干扰−回收"净值） | 避免参数堆积 |
| 不做（登记） | δ 的载荷依赖、偏航力矩耦合（金属环假设 H8 不动）、eta 过渡尾迹动力学 | 二阶量，P4 标定时一并处理 |

**版本绑定声明**：本增量构成 P2 模型 eta 路径 v1.1；T2 已关闭证据绑定其冻结提交的模型版本，数值不双向迁移（新增量不追溯改写 T2 结果，T2 数字不复用于新口径）。

## 3 交付物

1. **曲线族图**：η∈[0.75,1.1] 扫描 × {悬停, v\*=5, 8 m/s}，κ 敏感带叠加，逐条标谷底与深度（W 与 %）；
2. **η\*(v) 漂移图**：悬停 0.90 → 高速趋 1 的预测轨迹（标注"P4 共轴台架可验证项"）；
3. 指标小表（机器导出）+ 叙事段："共轴收益悬停最大（模型口径约 X%）、随前飞衰减；在线寻优与实测标定列为未来方向"；
4. 治理三件套（06 登记 / 清单 v1.3 增补 / worklog）。

## 4 门槛（预登记，跑批前冻结；正式判定以本表为准）

| # | 指标 | 门槛 |
|---|---|---|
| E1 | 悬停 P(η) 谷底位置 | ∈ [0.85, 0.95]（单谷） |
| E2 | 巡航 v\* P(η) 谷底位置 | ∈ [0.88, 1.00] 且 **≥ 悬停谷底**（衰减单调方向） |
| E3 | 单谷性 | 各工况扫描曲线唯一极小、无多峰伪影 |
| E4 | 深度量级（报告带） | 悬停深度与 δ 敏感带（κ∈[0.3,1.0]、δ₀∈[0.111,0.176]）随图报告，不设门（H5 假设依赖，不构成实测宣称） |
| E5 | G-ETA 门重锚 | 链检查原"eta≠1 功率上升"改为悬停点 **P(0.90)<P(1.00)<P(1.10)**（左支右支方向 + 谷底低于 1） |
| E6 | 无回归 | P2 链检查其余门全绿（悬停功率/饱和负向/恒等式不回归——改动仅限 eta 路径） |

## 5 实施步骤

1. `plane/config.m`：+`coaxial_decay_kappa=0.5`、映射斜率 0.3→0.55、删 `coaxial_delta_split_gain`（或置 0 并注明废弃）；
2. `plane/step.m`：δ_lo = δ(v_air)·应用（复用 local_ind_saving 内 vi 计算，提公用局部函数）；
3. `run_p2_chain_check.m`：G-ETA 重锚（E5）+ 全量复跑（E6）；
4. `run_eta_characteristics.m`（新增）：η 扫描 × 三工况 × κ/δ₀ 敏感带 → 图 1/2 + 指标表；
5. 证据 `docs/evidence/T24_ETA_CHAR_20260908/` + 状态页 + 双推。

预计半天（含复跑）。

## 6 边界

- η\*(v) 与曲线深度是**模型预测**（δ₀/κ 文献缺省），非实测宣称；升级路径 = P4 共轴对台架（P(s_up; T, v) 三维族一次实验定 δ₀、κ、η\*）；
- 全部结论锁 V1 桌面仿真；不做 eta 节能百分比的外推宣称；
- M3/px4_x8 与全部 `.slx` 零改动（冻结）；T2 证据不重开。
