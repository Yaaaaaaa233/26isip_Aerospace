# M2 第七轮独立验收报告（Codex，2026-09-02）

项目组：周航正、霍奕茗、于跃、叶安、王健祺

文件负责人：叶安（平台线）

主要撰写：Codex

技术贡献：Codex（远端与本地版本核对、MATLAB R2022b 分段复跑、c5 结果核对、聚合器负向验证）

审核：待项目组审核、待指导教师确认

AI 协助：Codex（第七轮独立验收执行与文档整理）

## 1. 结论

**本轮结论：R6 修复的代码逻辑有效，但当前环境下正式验收自动化仍为 PARTIAL，不能确认当前批次 42/42 完整闭环。M2 核心实现、ESC 接线、数值协议继续放行。**

当前主库提交为 `9cf9805008fd78ba52df94a9bca155fb53882401`，本轮未修改 `.slx`、算法实现或 M2 数值协议。仓库治理检查在 PowerShell 7 与 Windows PowerShell 5.1 下均通过。

在本机 MATLAB R2022b 中，按 `verify_m2_round4_closure` 分段模式实际复跑：

- `c1c2stale`：6/6 PASS；
- `c2clean`：3/3 PASS；
- `c3`：2/2 PASS，包含独立完整九场景试验；
- 四态契约与 manifest 负向正式 contract 未能进入，因为 c5 未完成阶段落盘；
- `report` 负向检查确认缺少 c5 时会硬失败，不会假绿。

c5 的两条最小链均实际完成，且两份 `summary.csv` SHA-256 完全一致；但 MATLAB 在第二条链结束后没有生成 `c5.csv` 与 `c5.done.mat`。因此本轮正式矩阵只能记为 **11/42 已完成且通过**，不能把两条链的原始结果替代为 c5 阶段 PASS。

本报告支持的结论限于当前 `air_spare.slx` 平台仿真代理对象；不支持真实八旋翼节能、偏航安全、SITL/HITL、实机或部署结论。

## 2. 版本、环境与范围

| 项目 | 值 |
|---|---|
| 主库当前提交 | `9cf9805008fd78ba52df94a9bca155fb53882401`（短哈希 `9cf9805`） |
| 本轮验证器 SHA-256 | `cbf098aaf38b5881c3319cbf3c33ff91192d4a3a08a6facd468cacaaf2683a0a` |
| MATLAB | R2022b，Windows，`-batch` 独立进程 |
| Simulink 对象 | `models/px4_x8/air_spare.slx` |
| 验收入口 | `verify_m2_round4_closure(stage)` |
| 本轮 runId | `dd8bda87-ee57-4969-8e1b-6cc846261501` |
| 证据等级 | `proxy`；平台功率为 `estimated` |
| 版本状态 | 本地 `main` 与 `origin/main` 一致，工作树干净 |

本轮只覆盖叶安负责的 `models/px4_x8` M2 平台线。Plane、M3、风场、RL 和实测数据不在范围内。

## 3. 分段矩阵实跑结果

| 阶段 | 结果 | 说明 |
|---|---:|---|
| `init` | PASS | 新建 manifest；记录 HEAD、验证器 SHA 和 clean tree |
| `c1c2stale` | 6/6 PASS | 单元错误出口、脏状态错误出口、compare/injection/trials 链错误出口 |
| `c2clean` | 3/3 PASS | clean 状态下三类链错误出口 |
| `c3` | 2/2 PASS | 独立 trials 受控失败与完整成功 |
| `c5` | 未完成 | 两条最小链均完成，但未生成阶段 CSV/done 标记 |
| `contract` | 未运行 | 按验证器设计依赖完整阶段证据，c5 缺失时不应伪造通过 |
| `report` | 负向 PASS | 明确拒绝缺少 `c5.csv` 的批次，错误 `air:M2Verify:StaleEvidence` |

当前已落盘的正式矩阵行数为 `6 + 3 + 2 = 11`；验证器 manifest 的声明总数为 42 行，因此只能报告 **11/42**。

## 4. 数值结果与 c5 复核

### 4.1 c3 九场景门槛

本轮 c3 产物中的 `[90,120] s` 窗口结果：

| 配对 | `delta_E_pct` | 相对 `+0.5%` 门槛裕量 |
|---|---:|---:|
| S1 | −0.262619229374685% | 0.762619 pp |
| S2 | −0.293803703864441% | 0.793804 pp |
| S3 | −0.214653662029612% | 0.714654 pp |

最小裕量为 `0.714654 pp`，大于登记的 `±0.015 pp` 会话抖动约 47 倍，不属于骑线通过。结果与第六轮登记值一致。

### 4.2 c5 两条最小链

本轮实际生成并核对的归档为：

- `results/air_m2_trials/20260902_181908/`；
- `results/air_m2_trials/20260902_182330/`。

两份 `summary.csv` 的 SHA-256 均为：

```text
D5FC25BF9C067DAC1620D62416EDB6235D7725B720C6E5C4B8BC2A764ED6F197
```

两份 `pairs.csv` 的 S1/S2/S3 数值均为 −0.262619% / −0.293804% / −0.214654%，说明最小双链的模型输出具有确定性；但由于阶段封装未落盘，该结果不能冒充 `c5` 阶段的机器可查 PASS。

## 5. R6 修复项复核

| 修复项 | 本轮判定 | 依据 |
|---|---|---|
| R6-F1：源码指纹独立取证、拒绝 dirty/unknown/不一致 | 代码逻辑通过；部分动态覆盖 | 当前 `init` 成功记录当前 HEAD/SHA/tree；各已运行 stage 生成 done 标记；完整 contract 负向未重跑 |
| R6-F2：聚合器逐行硬断言 verdict=PASS | 负向行为确认 | 当前批次缺 c5 时 `report` 硬拒绝；完整 `failrow` 篡改负向需随 contract 重跑 |
| R6-F3：c5 环境稳定性 | **仍开放** | 两条最小链结果完成且一致，但 c5 阶段文件仍未写出；未形成当前批次的可聚合证据 |

另外，MATLAB `checkcode` 报告的 GVMIS/INUSD 项为既有全局量契约与未使用形参提示，未发现本轮新增的语法错误；它们不替代动态验收。

## 6. 当前放行边界与下一步

- M2 受约束 eta 分配器、ESC 接线、旁路与修订数值协议：**继续放行**；
- 当前环境下验收基础设施总闭环：**PARTIAL**；
- 下一轮首先定位 c5 第二条链结束后未写出 `c5.csv`/`c5.done.mat` 的退出路径，并保留同会话确定性证明目标；
- 修复后必须从干净工作树重新执行 `init → c1c2stale → c2clean → c3 → c5 → contract → report`，以当前提交重新取得 42/42；
- 未完成上述重跑前，不得把历史 `2d36288` 批次的 42/42 直接当作当前提交的独立复验结果。

