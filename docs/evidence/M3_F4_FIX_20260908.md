# M3 F4 修复与验证证据（2026-09-08）

状态：**修复方自验完成（代码 + 非仿真套件全绿）；§7.3 正式批次复验 PASS（同日，见 §5）——M3 修复方侧证据链齐备，M3 放行仍待独立复验**
修复依据：[M3 第三轮独立复验报告](M3_REACCEPT_ROUND3_CODEX_20260905.md) §3.1–§3.4（四个已实证范围）与 §7（下一轮最小清单）；只修 F4，F2/F3 未重开，算法与参数未动。
修复执行：ZCode（Control/Simulink 平台线，叶安授权 P1/WP6）
涉及提交：`06a76f6`（F4 主体）→ `1398f29`（复核计划行解包）→ `e3eec09`（altLayout 夹具合并配置）→ `03719ed`（夹具 archiveDir 同步）→ `d552a14`（§7.3 正式批次冻结提交）

## 1. 修复→缺陷对应

| 第三轮 §3 缺陷 | 修复 | 代码位置 |
|---|---|---|
| §3.1 源码身份放宽：live HEAD ≠ manifest 提交仍 PASS；sha.aggregate 跳过断言只报 drift | 撤销豁免：聚合要求 live HEAD **等于**批次提交（`air:M3Agg:CrossCommit`，规则 §2 条款 5 语义：跨提交/跨版本拒绝；历史批次重分析须先按规则/ADR 升版）；manifest 声明的全部 7 个指纹（含 aggregator 自身）现场重算并逐一断言 | `m3_aggregate_batch.m` §2 段；指纹集合单一化到新文件 `m3_live_fingerprints.m`（init/聚合/夹具三方共用） |
| §3.2 正式入口漏检 done.attempts（六类全 PASS） | 包装入口 `m3_batch_aggregate` 拥有 done/result/marker **三方记账**：done.attempts 缺失/0/NaN/非整数/超冻结上限/与持久标记不一致 → `air:M3BatchAgg:StampAttempts`；stamp 的 runId/gitCommit/archiveDir 与 result 绑定（`StampIdentity`/`StampArchive`）；cfgSha 一致（`StampConfig`） | `m3_batch_aggregate.m`（`validateStampBookkeeping`） |
| §3.3 聚合不读归档配置、不复核臂档案 | ① 配置哈希三方链：`effective_config.mat` 文件 ↔ `result.cfgSha` ↔ done stamp（trials 端在冻结断言后计算并写入，`air:M3Agg:ConfigMissing/ConfigMismatch`）；② **14 臂生产路径复核**：每臂用归档日志 + 归档逐臂配置（含归档计划行）重跑唯一生产评价 `m3_eval_arm`，判定字段（ok/etaCenter/etaConv/attLimitMax/vTrk/etaReplayDiff）与归档摘要**精确一致**（`air:M3Agg:ArmRecheck`）；配置篡改由回放保真门（ReplayMismatch）拦截 | `m3_aggregate_batch.m` §2b/§6b；`run_air_m3_trials.m`（cfgSha） |
| §3.4 验证器入口不校验合同、盖章无身份、超预算可盖章 | 验证器入口在消耗预算**之前**调用 `m3_batch_validate`（被改上限的 manifest 在**每个阶段含 vreport** 死于 `air:M3Batch:ContractMismatch`，不消耗 attempt、不写 done）；验证器自身入口绑定写入盖章（binding/bindingExit/batchId）；vreport 增加记账审计：各前置阶段 done 齐全、attempts 在冻结上限内、盖章验证器指纹 == 现场文件 | `verify_m3_round3_closure.m` 入口/`auditVerifierBookkeeping` |

## 2. 验证结果（冻结提交 `03719ed`，干净树，开发 staged 副本 `results/air_m3_batch_staged/dev_round4c`）

### 2.1 内层聚合负向（R3-A，既有 25 项 + 2 正控）——25/25 精确 ID 拒绝

SourceMismatch / BatchId / DirtyArchive / ShaZero / ShaDelete / SegmentFailed / ArmFailed / SegmentTwice / SegmentUnknown / SegmentMissing / ManifestArms / ArchiveMissing / ReproSplit（合同层）/ ReproTimeGrid / ReproGridLen / Attempt 六类（BadAttempts）/ CapRaiseSynced / SegRemove / SegDuplicate（合同层）/ ManifestSha（合同层）——全部落在声明的专属错误 ID；正控：原样副本 PASS + 三段替代布局 PASS（含 14 臂复核）。

### 2.2 正式入口假绿复跑（R3-AW，第三轮报告 §7.2 的 11 项 + 包装正控）——12/12

| 探针 | 结果 |
|---|---|
| done.attempts 六类（删字段/0/NaN/1.5/4 超上限/2 与标记不一致） | 全部 `air:M3BatchAgg:StampAttempts` |
| 聚合器 hash 两类（删 sha.aggregate / 置 64 零） | `air:M3Agg:ContractMismatch` |
| 配置浅篡改（M3-N1 eta gain 2e-4→1） | `air:M3Agg:ConfigMismatch`（哈希层） |
| 配置深篡改（同上 + 重 stamp 全部哈希） | `air:M3EvalArm:ReplayMismatch`（生产复核回放保真门） |
| 单臂失败归档（摘要 ok=true + 臂文件失败 + 姿态日志 [210,214) 注位 3） | `air:M3EvalArm:ReplayMismatch` |
| 验证器超上限（manifest.maxAttempts 3→4 + vreport） | 合同层精确拒绝；**无 vreport 盖章、无 attempt 消耗** |
| 包装正控（正式入口全聚合） | PASS |

### 2.3 真实批次在新规则下的聚合（严格绑定 + 14 臂复核）

`M3 AGGREGATE BATCH PASS (batchId 9b7a6980…, 5 segments, 14 arms, commit 03719ed…)`（正控副本，身份按夹具规则重 stamp 至冻结提交）：

- 14 臂复核：9 个 eta 臂 replayDiff = 0、中心值与归档摘要逐位一致；4 个基线臂（B0/B1，无 eta 回放）复核一致；全部 ok=1 与摘要一致；
- 配对门槛重算与第三轮报告 §6 数值一致（M3-N1…N5 vs B0-N：−0.29102/−0.29272/−0.29195/−0.29402/−0.27789 %，B1 公共掩码 4800/96001≈5.0%，B2 65601/96001≈68.33%）；裕量沿用第三轮独立口径（B1 最小 0.777065 pp @N3，B2 最小 0.496826 pp，14 臂中心最小裕量 0.001396 @N4 / 覆盖 B2 时 0.000978 @B2-N2），本轮未产生新数值；
- repro：M3-R1 vs M3-N5 同会话、时间向量全等、max|d eta| = max|d v| = 0。

### 2.4 验证器自身

- vunit：三套单测 PASS（新盖章格式含验证器绑定；审计行 "stamped by this source"）；
- vaggregate：上述全套，`STAGE vaggregate PASS`（attempt 2/3，真实计数）；
- vreport 记账审计：**拒绝不完整验证链**（vnegative 无 done 盖章 → `air:M3Verify:Bookkeeping`）——审计按设计工作；vnegative（约 20 个 240 s 仿真 + R2022b 堆崩溃风险）与正式 14 臂批次复跑按 §7.3 与正式批次同批执行。

## 3. 如实记账与环境边界

- 开发期间三次过程级失败如实消耗 scratch 副本 attempt：复核计划行解包崩溃（`1398f29` 修复）、套件运行期间工作树被并行文档写入弄脏触发 DirtyTree（流程错误，非代码缺陷——修复代码后重跑）、夹具 archiveDir 不一致（`03719ed` 修复）。scratch staged 副本（dev_round4b/c）非批次证据，计数未重置；
- 夹具身份为"合成现役身份"（`restampFixture`：manifest 提交/指纹、段绑定、cfgSha、done 同步重 stamp），身份类负向在重 stamp **之上**施加篡改，保证拒绝归因于被改字段而非批次年代；真实旧批次（ec171b5）在新规则下不可再聚合，属规则 §2 条款 5 的登记语义；
- 本轮零 Simulink 仿真；§7.3 正式复验（干净冻结提交、入库驱动、正式 14 臂 + 声明的四态恢复矩阵 + vnegative + 受影响回归）未做，M3 整体放行仍以其为准。

## 5. §7.3 正式批次复验（2026-09-08，冻结提交 `d552a14`，干净树，入库驱动）

按第三轮报告 §7.3 执行：`tools/run_m3_batch.ps1`（MaxAttempts=3，R2022b）在冻结提交 `d552a14` 干净树上一体跑完 **init → s1..s5 → aggregate → vunit → vnegative → vaggregate → vreport**，**11/11 阶段全部 attempt 1 PASS**（无重试、无崩溃、无预算消耗；日志与新鲜标记完整保留于 `results/batch_runs/20260907_183404/` 与 `results/air_m3_batch_staged/20260907_183404/`，results/ 不入库）。

- **正式 14 臂批次**（batchId `1d9dd7aa…`，manifest 提交 = live HEAD = `d552a146…`）：5 段 14 臂全 ok；聚合含 §3.1–§3.3 全部新规则——live HEAD 严格等于批次提交、全部 7 指纹现场重算、done/result/marker 三方记账、归档配置三方哈希链、**14 臂生产复核逐臂重跑 `m3_eval_arm` 与归档摘要精确一致**（9 个 eta 臂 replayDiff=0，4 个基线臂无 eta 回放，B2 两臂 replayDiff=0）；
- **配对门槛与第三轮独立数值一致**（本批零新数值）：M3-N1 vs B0-N 掩码 −0.29102%（门槛 0.5%，common 65601/96001≈68.33%）；中心值/converged/monotonic/maxReg 与第三轮报告 §6 同；
- **vnegative 四态恢复矩阵**：边界试验（M3 BOUNDARY TRIAL PASS，out-of-band 负向 + 恢复态 restore）+ **恢复矩阵 28 行全过（trials 4×5 + boundary 4×2）**；R3-M 边界入口 2 次运行成功且状态 restored；
- **验证器记账审计**：vunit/vnegative/vaggregate 三阶段盖章 attempt 均为 1/3、验证器指纹 = 现场 `d552a14`、"stamped by this source"；vreport 记账审计 PASS；
- **受影响回归**：`tools/test_m3_batch_driver.ps1` 9 场景 PASS（重试类别/标记新鲜度/启动器 rc 不信任/归档目录证据/GBK 日志往返）；`test_m3_coordination_unit` PASS。

§7.3 清单逐项对照：①干净冻结提交 ✔（`d552a14`）；②入库驱动 ✔；③正式 14 臂 ✔；④声明的四态恢复矩阵 ✔（28 行，行数与声明一致）；⑤受影响回归 ✔；⑥验证器属于该证据版本 ✔（同批同提交盖章）；⑦失败与新鲜标记保留、未清计数 ✔（attempt 全 1，无任何手动重置）。

## 6. 结论边界（更新）

本文件是**修复方自验**：F4 四个已实证范围的修复与全部负向/正控证据成立；§7.3 正式批次复验（正式 14 臂 + 四态恢复矩阵 + vnegative + 受影响回归）已在冻结提交 `d552a14` 一体完成并 PASS，M3 修复方侧证据链齐备。**M3 整体放行仍以独立复验为准**（修复方自验 ≠ 独立验收）；不宣称"完全闭环"、不将代理结果表述为统一 Plane/实飞结论（§9.4/ADR-003）。真实旧批次（ec171b5）在新规则下不可再聚合，属规则 §2 条款 5 的登记语义。
