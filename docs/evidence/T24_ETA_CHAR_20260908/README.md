# T2.4 eta 特性展示·正式批证据（2026-09-08）

项目组：周航正、霍奕茗、于跃、叶安、王健祺
文件负责人：叶安（Control / Simulink 平台线）
主要撰写：叶安（2026-09-08；ZCode 辅助整理初稿）
技术贡献：叶安（T24 三项拍板：δ(v) 形式与 κ=0.5、曲线族演示替代单条、寻优平台冻结）；ZCode（模型 v1.1 实施、正式批与证据整理）
AI 协助：ZCode（本目录证据生成与入库）
审核：叶安已按人工验收包 H1–H9 核对通过（2026-09-08，"没有问题"）；本批为正式证据，不代替项目组 S0 签批

> **状态：T2.4 验收关闭（2026-09-08）**——E1–E6 机器门全 PASS + 人工 H1–H9 核对通过。P2/T2 平台线交付至此全部完成。

## 1. 范围与结论

按 [`T24_ETA_CHAR_PLAN.md`](../../T24_ETA_CHAR_PLAN.md) v1.0（commit a7885ca）完成 **P2 eta 路径模型 v1.1** 与特性演示正式批：

- **E1–E6 全部 PASS**（E1/E2/E3 由 `run_eta_characteristics.m` 机器门，E5/E6 由 `run_p2_chain_check.m` 独立承担，同工作树运行）；
- 核心结果：悬停谷底 **η\*=0.899**（文献锚 ~0.90），v\*=5 m/s **0.941**，8 m/s **0.950**，η\*(v) 0..12 m/s 单调向 1 回漂（12/12 段非降，终点 0.956）；
- 悬停 P(η=1)=487.3 W 与 T2 冻结口径**逐位一致**（v=0 时 δ(v)=δ₀、诱导节省=0 的设计保证，悬停不回归）；
- eta 在线寻优不作为阶段目标（D1：原 M3 十四臂统一 Plane 复跑登记性取消；D2：px4_x8 寻优机器按已放行状态冻结）。

## 2. 模型增量（v1.1，全部登记于 06 方案 v1.3 H5 行）

| 项 | v1.0 | v1.1 |
|---|---|---|
| 下桨干扰 | δ=δ₀ 常数 + 回中项 +0.4·(s_up−0.5) | **δ(v)=δ₀·(vi(v)/vi₀)^κ**（κ=0.5，复用 H3 vi 函数；回中项删除） |
| 分配映射 | s_up=0.5+0.3·(1−η) | s_up=0.5+**0.55**·(1−η) |
| 谷底位置 | 人为钉在 η=1（回中项构造） | **模型输出**（物理自然给出） |

改动文件：`models/plane/+plane/config.m`（κ 参数、斜率 0.55、删 `coaxial_delta_split_gain`）、
`models/plane/+plane/step.m`（δ_lo=δ(v_air)，vi 提公用局部函数）、
`models/plane/run_p2_chain_check.m`（G-ETA 第三门重锚为悬停三点序）、
`harness/+harness/make_plane_adapter.m`（真值图同步 v1.1 同式，保持 G1 门 1% 容差可信）、
`models/plane/run_eta_characteristics.m`（新增正式批入口）。

## 3. 门槛与实测（T24 方案 §4 跑批前冻结）

| # | 指标 | 门槛 | 实测 | 判定 |
|---|---|---|---|---|
| E1 | 悬停 P(η) 谷底 | ∈[0.85,0.95] 单谷 | **0.899** | PASS |
| E2 | v\*=5 谷底 | ∈[0.88,1.00] 且 ≥悬停 | **0.941** | PASS |
| E3 | 单谷性（三工况） | 唯一极小 | 3/3 | PASS |
| E4 | 谷深/敏感带 | 报告带不设门 | 悬停 1.89 W/0.39%；v\* 0.56 W/0.16%；8 m/s 0.36 W/0.09%；κ∈[0.3,1.0] 谷底带 悬停恒 0.899 / v\* [0.933,0.958] / 8 [0.939,0.969]；δ₀=0.176 上沿 0.845/0.909/0.922 | 报告 |
| E5 | 链检查悬停三点序 | P(0.90)<P(1.00)<P(1.10) | 485.41 / 487.30 / 492.86 W | PASS |
| E6 | 链检查无回归 | 其余门全绿 | **24/24 门 PASS**（含悬停 488.7 W 锚、U 形 v\*=5 降幅 27.3%、饱和负向、恒等式全绿）；适配器检查 5 门 PASS（G1 最大偏差 0.48%<1%） | PASS |

补充观察（R 类，报告）：η\*(v) 12/12 段非降；悬停 κ 带宽 max|ΔP|=0.0 W（形式自检：v=0 不衰减）。

## 4. 运行口径

- settle 仿真协议与链检查 G-U 同源（圆 R=100 m、W=0、满电 96 Ah、eta 快速到达 τ=1e-3/rate=100、|v−v_ref|≤0.05 连续 4 s 后取 0.5 s 静默窗 power_demand 均值）；
- 扫描网格：η∈[0.75,1.1]（缺省 0.01 / 敏感带 0.02 分辨率），漂移图 v∈0..12 m/s；
- 两次正式批（首跑与图表呈现打磨后重跑）**数值逐位一致**（确定性仿真）；
- 正式产物目录（gitignored，不入库）：`results/eta_characteristics/20260908_142954_eta_char/`（图 1/图 2/指标表/result.mat）与 `results/t24_20260908/`（两次运行日志）；
- 人工验收包（仓库外，不入库）：`第二阶段\T24人工验收包_20260908\`（含 H1–H9 核对单）。

## 5. 边界

1. η\*(v) 与谷深是**未标定模型预测**（δ₀/κ 文献缺省等级），不构成实测节能宣称；升级路径 = P4 共轴对台架 P(s_up;T,v) 三维族一次实验定 δ₀、κ、η\*；
2. 全部结论锁 V1 桌面仿真；T2 已关闭证据绑定其冻结时模型版本，本增量不追溯改写 T2 数字（版本绑定声明见 T24 方案 §2）；
3. M3/px4_x8 与全部 `.slx` 零改动（D2 冻结）；算法线 eta 对口基线（coaxial_ratio_opt w11）由算法线自行引用本目录锚点，不改动本模型。
