# M2 第七轮发现修复报告（R7-F1，2026-09-02）

项目组：周航正、霍奕茗、于跃、叶安、王健祺

文件负责人：叶安（平台线）

主要撰写：ZCode

技术贡献：ZCode（R7-F1 修复实现、崩溃重试语义设计、无仿真探针与进程击杀模拟、正式批次执行）

审核：待项目组审核、待指导教师确认

AI协助：ZCode（修复实现与本文档代拟）

## 1. 结论

**第七轮发现（c5 阶段在两条最小链完成后、阶段落盘之前发生 MATLAB R2022b 原生进程崩溃，导致 11/42）已按"受控韧性 + 诚实记账"修复并经完整批次关闭验证：本批次 42/42 针对性矩阵 PASS，全部阶段第 1 次尝试完成（无崩溃重试发生），双链哈希一致，门槛值在登记抖动内。M2 核心实现、ESC 接线与修订数值协议继续放行；验收自动化在"当前提交已取得完整 42/42 矩阵证据"的意义上恢复。**

崩溃本身是环境缺陷（R2022b 堆损坏，崩溃点随机：4/8/45 次仿真深度、以及本轮记录的"链完成后落盘前"与"写完 manifest 后进程挂起不退出"两种新表现），代码逻辑无法免疫；本修复不宣称消灭崩溃，而是使批次能够**有界、完整重执行、如实记账**地穿越崩溃。

本报告支持的结论限于当前 `air_spare.slx` 平台仿真代理对象；不支持真实八旋翼节能、偏航安全、SITL/HITL、实机或部署结论。

## 2. 修复项

| 修复项 | 内容 | 提交 |
|---|---|---|
| R7-F1a | 每个段入口在源码绑定门通过后、任何工作开始前，递增持久的 `<stage>.attempts` 计数器（存于 gitignore 的 staged 目录，不触碰脏树门保护的源码绑定）；完成尝试把计数写入 `done.attempts`，report 聚合逐段打印并随 result.mat 归档 | `35436ec` |
| R7-F1b | c5 两条链完成后、进入哈希/落盘窗口前 `bdclose('all')` 释放 Simulink 堆（第七轮崩溃取证恰好落在该窗口；脏会话主张由已完成的链建立，关模型不影响） | `35436ec` |
| R7-F1c | 规则 v1.4 §2 规则 7：段进程崩溃重试语义——仅限"非零退出且未写出 done 标记"、从段入口完整重执行（禁止从进程内部分状态续传）、次数上界（本批次驱动声明上限 3 次）、计数器诚实记账、盖章后退出崩溃无害 | `fd4ce7c` |

批次驱动脚本（含重试逻辑）按惯例置于仓库外（`Temp/run_r7_batch.sh`），其重试行为受规则 v1.4 约束，实际尝试数由 staged 目录计数器与驱动日志双重可查。

## 3. 无仿真探针与崩溃模拟（修复提交后、正式批次前执行）

| 探针 | 结果 |
|---|---|
| `'fail'` 钩子触发且调用者全局逐域恢复（哨兵有限值） | PASS |
| 未知段名以 `air:M2Verify:BadStage` 拒绝 | PASS |
| 干净树 `init`：manifest 绑定 `fd4ce7c`、声明 42 行、staged 目录无任何残留 attempt 标记 | PASS |
| **进程击杀模拟 1**：启动 c5 段，attempt 标记约 15 s 落盘（值 1）后强杀 MATLAB 进程——标记存活、`c5.csv`/`c5.done.mat` 未生成（精确复现第七轮崩溃现场状态） | PASS |
| **进程击杀模拟 2**：再次启动 c5 段，标记递增 1→2，强杀后仍无任何阶段证据落盘 | PASS |
| 脏树顺序钉：脏树下运行 `c1c2stale` 被 `air:M2Verify:DirtyTree` 拒绝且**未**写 attempt 标记（计数只发生于通过源码绑定门的真实段执行），先前标记完好 | PASS |

击杀模拟说明：强杀进程（taskkill /F）与原生崩溃在计数器语义上等价——标记在段入口、任何仿真之前落盘，进程死亡不影响其存在；两次模拟证明"崩溃 → 计数存活 → 重试递增 → 无假证据"的完整链条。

## 4. 正式批次数据

| 项目 | 值 |
|---|---|
| runId | e8011d58-4f5e-40cd-a825-031de188f040 |
| 绑定提交 | `fd4ce7c`（HEAD = origin/main，单一提交批次绑定） |
| 验证器 SHA-256 | `d7191c16aa7a2f8e6e5e409202c8a3dbb4afdcfce3bb451de8377e6a6224185e` |
| 批次驱动 | `Temp/run_r7_batch.sh`（重试上限 3，逐尝试日志 `results/round7_run/`） |
| 各段尝试数 | c1c2stale=1、c2clean=1、c3=1、c5=1、contract=1（全部第 1 次尝试；计数器/驱动日志双重可查） |
| 矩阵结果 | 42/42 PASS（c1c2stale 6、c2clean 3、c3 2、c5 4、contract 27） |
| c5 双链 summary.csv SHA-256 | `d5fc25bf9c067dac1620d62416edb6235d7725b720c6e5c4b8bc2a764ed6f197`（两链一致；与第六轮批次、第七轮验收双链跨批次一致） |
| c5 门槛 dE（nominal S1/S2/S3） | −0.262619% / −0.293804% / −0.214654%（S1/S2/S3，与登记值一致；最大变化 0.01152 pp < ±0.015 抖动带；对 +0.5% 门槛最小裕量 0.714654 pp ≈ 48× 抖动） |
| 治理检查 | PowerShell 5.1 PASS（12 模块 / 24 Markdown） |


## 5. 边界与未覆盖组合

- 全量 9 场景**同会话**双链仍为环境限制下的未覆盖组合（R2022b 堆损坏深度；R6-F3 登记口径不变）；全量场景由 c2clean/c3 每进程覆盖一次。
- 重试语义是环境缺陷的受控韧性，不是崩溃的消除：本批次各段第 1 次尝试即完成，**重试路径本身未在正式批次中被触发**（其语义由两次进程击杀模拟动态证明）；若未来批次发生真实崩溃重试，证据文档必须报告实际尝试数与崩溃尝试日志位置。
- 阶段成功入口态、第三种入口态（错误后残留）、干净入口双链、成功退出×干净入口等组合仍不在针对性矩阵内（R6 口径不变）。
- 历史批次（`2d36288` 的 42/42、`bb3097a` 时的 11/42）不替代本批次证据；本批次之后 HEAD 再前进仍需整批重跑（单提交绑定，规则 v1.3 §2 规则 5）。

## 6. 诚实执行记录

- 探针首轮两处运行目录失误（`run()` 切 cwd 至 Temp、从仓库根启动缺路径）：报 `UndefinedFunction`，改为绝对路径/模块目录启动后通过；未影响任何仓库文件。
- 探针期间发现并清理 Codex 第七轮批次遗留的挂死 MATLAB init 进程（PID 2212，18:07 启动、manifest 已写出但进程两小时未退出）——环境不稳定的又一表现，已按无害处理（其证据已完成，且与本轮修复无关）。
- 击杀模拟在正式批次之前完成；正式批次 init 清空 staged 目录（含模拟留下的计数器），批次从零计数开始。
- **c3 活案例**：c3 进程在写出 c3.csv/c3.done.mat **之后**以 `0xc0000374, Heap corruption` 退出（驱动 rc=127）——证据已原子完整，驱动按规则 v1.4(e) 放行且无需重试；这是"盖章后退出崩溃无害"条款的正式批次内直接动态证据。
- 击杀模拟 1 的 bash 后台 PID 是启动器而非 MATLAB 本体，首次 taskkill 未命中目标，改按命令行定位真实 MATLAB 进程后击杀；该失误只影响探针执行方式，不影响语义验证。
- （批次期间如有其他自发现缺陷，如实追加）

## 7. 状态口径

- M2 核心实现、ESC 接线、修订数值门槛：继续放行。
- 验收自动化：当前提交 `fd4ce7c` 已取得完整 42/42 针对性矩阵证据（第七轮的 11/42 已被本批次关闭）；崩溃重试语义入规则 v1.4。
- 措辞纪律：不使用"完全闭环/永不崩溃"；环境缺陷以有界韧性 + 诚实记账应对，全量同会话双链仍登记为环境限制未覆盖组合。
