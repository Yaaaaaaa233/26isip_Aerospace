# M2 第二轮复验问题修复与关闭验证（ZCode，2026-09-01）

结论：**第二轮复验问题 R2-F1 / R2-F2 / R2-F3 全部修复，并按第二轮报告 §9 的关闭条件逐条验证通过。**M2 数值结论（120 s 协议、[90,120] s 门槛窗、±0.5% 门槛）不受本轮影响、继续有效。执行对象：[`M2_REACCEPT_ROUND2_CODEX_20260901.md`](M2_REACCEPT_ROUND2_CODEX_20260901.md)。

项目组：周航正、霍奕茗、于跃、叶安、王健祺
文件负责人：叶安
主要撰写：叶安（修复方案与关闭判定）、ZCode（修复实现、验证执行与本文档代拟）
审核：待项目组审核、待指导教师确认
AI协助：ZCode（状态管理修复、验证链实现与本文档代拟）

## 1. 修复内容

| 项 | 修复 |
|---|---|
| R2-F1（高，Z1 重开） | `run_m2_session_chain`：入口**规范化** M2 会话状态为安全恒等态（`M2_ETA_PARAMS = fixed/1.0`、`M2_ETA_APPLIED = 1.0`、`clear('m2_eta_esc')`），调用者入口状态经 `onCleanup` 在退出（成功/异常）时恢复；`run_air_m2_trials` 同样自带快照/恢复，不再成为陈旧状态源。快照/恢复语义与单元测试一致：入口为空则恢复为空（各消费者的安全恒等缺省） |
| R2-F2（高） | 链尾新增 `assert(result.pass, 'air:M2Session:TrialsFailed', ..., result.archiveDir)`；九场景失败时链以错误退出并携带归档路径，不再可能打印 END 后假绿 |
| R2-F3（中） | 单元测试的恢复与 `clear('m2_eta_esc')` 合并为**单一 onCleanup 回调**（`m2test_cleanup`），成功路径与任何 assert/错误路径执行同一清理 |

## 2. 关闭条件验证（第二轮报告 §9）

单 MATLAB 会话一次完成全部条件（2026-09-01 23:42--23:46）：

| 条件 | 验证方法 | 结果 |
|---|---|---|
| 1. 链内自动规范化 + onCleanup 恢复，不要求手动清理 | 会话入口注入第二轮报告 §4 的**原样脏状态**（`M2_ETA_PARAMS = esc/1`，`M2_ETA_APPLIED = 0.99914776890319873`），直接 `run_m2_session_chain` | **PASS**：单元测试 PASS、旁路比较差 0、注入 4/4、九场景 PASS（归档 `results/air_m2_trials/20260901_234210`）——第二轮 §4 的 FAIL 复现在规范化下不再发生 |
| 2. 同一会话连续两次完整链 2/2 通过，第二次直接承接第一次 `run_air_m2_trials` 留下的历史 | 链 1 结束后不加任何清理立即运行链 2 | **2/2 PASS**（归档 `results/air_m2_trials/20260901_234533`），链间零人工干预 |
| 3. 链尾断言 `result.pass`，并以可控非破坏性测试证明失败路径返回错误 | 链尾同一断言表达式喂入 `pass=false` 的构造 result | **断言触发**：`air:M2Session:TrialsFailed: M2 trials failed, archive proof/nondestructive`（未改任何仓库文件） |
| 4. 单元测试异常路径同时恢复 global 并清 persistent | 单一 `m2test_cleanup` 回调挂在 onCleanup 上（成功路径显式触发、错误路径兜底） | 代码审查 + 单元测试重跑（两链中均 PASS） |
| 5. 勘误过强表述 | `M2_REACCEPT_FIX_20260901.md` 末尾追加勘误节（原文保留）；DEVELOPMENT_STATUS 措辞同步 | 见两文档 |

两次链的九场景数据逐位一致（门槛窗 S1/S2/S3 = −0.25985% / −0.29211% / −0.22617%），与修复报告 3 会话及第二轮干净复跑四方一致——规范化后链内状态确定，数值不受入口历史影响。

## 3. 边界

- M2 数值门槛结果继续有效（第二轮报告 §5 的干净状态复验与修复报告 3 会话逐位一致）；±0.5% 门槛未动；快照 `air_m2.slx` 未变。
- 本轮全部为验收编排与会话状态管理修复，不改变模型与算法。
- M3 的结构集成约束照第二轮报告 §10：在 R2-F1/R2-F2 关闭后可开始；本文件即关闭记录。

## 4. 第三轮勘误（2026-09-02，Codex 独立复验）

本文件保留 2026-09-01 当时的修复与验证记录，但“成功或异常均恢复”“R2-F3 错误路径关闭”的结论已被第三轮真实错误注入修订。第三轮确认脏入口背靠背正常链 2/2 与链尾硬断言成立，同时复现：单元测试和完整链在真实断言错误后均未恢复调用者入口 global。当前状态与关闭条件以 [`M2_REACCEPT_ROUND3_CODEX_20260902.md`](M2_REACCEPT_ROUND3_CODEX_20260902.md) 和 [`../DEVELOPMENT_STATUS.md`](../DEVELOPMENT_STATUS.md) 为准。

本节技术贡献：Codex（受控失败复验与历史结论勘误）；审核仍为待项目组审核、待指导教师确认。
