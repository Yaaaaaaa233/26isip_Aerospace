# M2 第四轮复验问题修复与关闭验证（ZCode，2026-09-02）

结论：**第四轮复验问题 R4-F1 / R4-F2 / R4-F3 全部修复，第四轮报告 §6 关闭条件 1--5 逐条验证通过。**总验证器 `verify_m2_round4_closure` 现与其执行的契约一致：恢复调用者全局（成功与错误路径）、persistent fresh 有直接动态证据、矩阵为逐行声明的**针对性矩阵**（15 行，未覆盖组合明确列名）。M2 核心成果维持放行（第四轮报告 §7）；后续里程碑将其用作放行门前，本修复即为前置条件关闭。执行对象：[`M2_REACCEPT_ROUND4_CODEX_20260902.md`](M2_REACCEPT_ROUND4_CODEX_20260902.md)。

项目组：周航正、霍奕茗、于跃、叶安、王健祺
文件负责人：叶安
主要撰写：叶安（修复方案与关闭判定）、ZCode（验证器合规改造、关闭验证执行与本文档代拟）
审核：待项目组审核、待指导教师确认
AI协助：ZCode（验证器修复、驱动执行与本文档代拟）

## 1. 修复内容（第四轮报告 §5）

| 项 | 修复 |
|---|---|
| R4-F1（高） | `verify_m2_round4_closure` 入口快照调用者两全局，函数帧 `onCleanup` 在成功与异常路径恢复；删除原"末尾清空全局"及其误导注释（清 persistent 保留——报告明示允许，且不等价于恢复全局）；新增 `'fail'` 自注入钩子（入口快照与全局触碰之后、矩阵之前触发 `air:M2Verify:InjectedFailure`），验证器自身的错误路径恢复可被真实注入 |
| R4-F2（中） | 把第四轮 §4.3 的 persistent 直探固化进 C1b：`warmPersistent`（合成输入驱动 `m2_eta_esc` 至非初始持久态）→ 哨兵全局 → 注入单元测试失败 → 错误后首次控制器输出 vs 显式 `clear` 后首次输出，`maxDiffFresh` 必须为 0，写入矩阵行与 `result.mat` |
| R4-F3（中） | 双管齐下（报告给出两条路径，均执行）：① 矩阵扩展——三类链错误出口与单元测试错误出口各补 **clean 入口态**变体（原仅 stale 哨兵）；② 措辞收窄——本文与验证器头注释均命名为"**针对性矩阵**"并逐行列明 case，`ACCEPTANCE_AUTOMATION_RULES.md` §4.3 澄清为 v1.1（矩阵范围 = 缺陷类别相关的全部组合；声明行数 = 实际行数；未覆盖组合必须命名"针对性"并列明） |

## 2. 验证覆盖范围（针对性矩阵，15 行）

| case | 入口状态 | 出口 | 验证内容 |
|---|---|---|---|
| C1a unit 受控失败 | clean | assert | 全局恢复为空（安全缺省） |
| C1b unit 受控失败 | stale 哨兵 | assert | 全局逐域恢复 |
| C1b persistent 直探 | stale 哨兵 | assert | 错误后首输出 ≡ fresh 首输出（maxDiffFresh=0） |
| C2 compare 错误出口 ×2 | clean / stale | assert | 全局逐域恢复 |
| C2 injection 错误出口 ×2 | clean / stale | assert | 全局逐域恢复 |
| C2 trials 错误出口 ×2 | clean / stale | assert | 全局恢复 + 消息含归档路径 |
| C3 试验独立受控失败 | stale 哨兵 | 正常返回 | 全局逐域恢复 |
| C3 试验独立完整成功 | stale 哨兵 | 正常返回 | 全局逐域恢复 |
| C5 脏会话双链 ×2 | 第二轮原样残留 / 链 1 试验残留 | 成功 | 2/2 PASS |
| C5 同会话 CSV 确定性 | 链 1 试验残留 | 成功 | 两链 summary.csv SHA-256 互等 |
| C5 门槛抖动容差 | n/a | 成功 | [90,120] ΔE% 在第三/四轮值 ±0.015pp 内 |

**明确未覆盖**（针对性矩阵边界，如实列明）：成功出口的 clean 入口态、每类错误出口的"上次错误退出后"第三种入口态、双链的 clean 入口态——这些组合与本轮修复的缺陷类别（验证器自身状态契约、错误路径恢复）不直接相关，未执行。

**声明**：矩阵共 15 行（case 级），归档 `matrix.csv` 行数与本表一致。

## 3. 关闭条件验证（第四轮报告 §6）

**执行方式（如实记录）**：验证器改为**分段执行**（`verify_m2_round4_closure(stage)`，stage ∈ fail / c1c2stale / c2clean / c3 / c5 / report）——当天单进程长序列多次在 MATLAB **进程级堆损坏**（0xc0000374）崩溃，崩溃深度完全不相关（第 4/8/45 次重仿真不等，GUI 会话在与并发无关的时段同样跑通），判定为本机 R2022b 环境抖动；分段后每段独立进程、段内仿真数 ≤30。C5 双链保持在同一段进程内（同会话历史是 h1==h2 口径的前提）。驱动 `tmp_stage_check.m`（临时，以非空调用者哨兵 `{esc,0.8123,tag}`/`0.8123456789` 进入每段，段后断言调用者状态逐域恢复）。

| 条件 | 验证 | 结果 |
|---|---|---|
| 1 非空调用者状态：正常返回与受控异常后两全局逐域恢复 | `'fail'` 注入 → 捕获 `air:M2Verify:InjectedFailure` → 恢复（COND1A）；**每段**正常返回后恢复（COND1B × c1c2stale/c2clean/c3/c5） | **PASS（5 项）** |
| 2 persistent fresh 直接输出比较入验证器与归档 | C1b 前向时间直探 `maxDiffFresh=0` 入矩阵行与 `result.mat`；灵敏度自检（故意不清 drifted 状态同探针必须测出差异）通过 | **PASS** |
| 3 矩阵扩展或措辞收窄，声明与行数一致 | 两者都做：clean 变体 4 行 + 全部措辞改"针对性矩阵" + 规则 v1.1；聚合段断言 `size(rows,1)==15` | **PASS（15/15）** |
| 4 重跑验证器：2/2 链、同会话哈希一致、±0.015pp 口径 | 双链归档 `results/air_m2_trials/20260902_120030`、`20260902_120349`，2/2 PASS；summary.csv SHA-256 互等 `f5fcb798…`；门槛值 −0.2626/−0.2938/−0.2147% 全在第三/四轮值 ±0.015pp 内 | **PASS** |
| 5 六处文档一致 | 统一为"核心 M2 与总验证器均按第四轮关闭条件修复并复验"，不用"完全闭环"全称 | 提交 diff |

矩阵 15 行全 PASS，聚合归档 `results/round4_closure/20260902_121040/matrix.csv`。过程如实记录：c3 段第一次尝试的全部检查与恢复断言已通过后，MATLAB 在**进程退出阶段**堆损坏（rc=127，工作已完成）；第二次尝试干净通过（rc=0）。段聚合另修正两处驱动侧缺陷（readtable 未指定 Delimiter 把空格当分隔符、行数断言误用 numel 数到单元格元素）。

## 4. 复盘

- R4-F1 的教训写进规则 v1.1 §6：**验收基础设施自身**的状态契约也要有直接动态证据——"执行契约的工具遵守契约"不能靠自明，第四轮的验证器探测（§4.4）就是对该假设的实测。
- 三次措辞强于覆盖（R2"无需清理"、R3"成功或异常均恢复"、R3 修复"全矩阵"）均为同一失败模式；v1.1 把"声明行数 = 实际行数、未覆盖组合必须列名"写死为规则。
