# M2 第三轮复验问题修复与第四轮关闭验证（ZCode，2026-09-02）

结论：**第三轮复验问题 R3-F1 / R3-F2 / R3-F3 全部修复，第四轮关闭条件（第三轮报告 §9 六条）全部验证通过；随修复同轮落地的《验收自动化基础设施规则》（`docs/ACCEPTANCE_AUTOMATION_RULES.md` v1.0）覆盖矩阵全部 PASS。M2 恢复放行。**执行对象：[`M2_REACCEPT_ROUND3_CODEX_20260902.md`](M2_REACCEPT_ROUND3_CODEX_20260902.md)。

项目组：周航正、霍奕茗、于跃、叶安、王健祺
文件负责人：叶安
主要撰写：叶安（修复方案与关闭判定）、ZCode（函数化改造、验证矩阵实现与本文档代拟）
审核：待项目组审核、待指导教师确认
AI协助：ZCode（入口函数化、错误注入钩子、验证矩阵执行与本文档代拟）

## 1. 修复内容（第三轮报告 §8）

| 项 | 修复 |
|---|---|
| R3-F1（高） | `run_m2_session_chain` / `run_air_m2_trials` / `test_m2_eta_esc_unit` 三入口**脚本改函数**：函数帧的 onCleanup 在正常返回与异常栈展开时都可靠执行，"成功或异常均恢复"从声明变为可验证事实 |
| R3-F2（高，R2-F3 重开） | 单元测试的全局恢复与 `clear('m2_eta_esc')` 合并为单一 `m2test_cleanup` onCleanup 回调，错误路径由函数帧兜底 |
| R3-F3（中） | 三入口各加**受控错误注入钩子**（`'unit'/'compare'/'injection'/'trials'`，缺省不激活），并新增 `models/px4_x8/verify_m2_round4_closure.m` 把全部错误出口真实注入一遍、自动断言入口状态恢复 |
| 流程（用户决议） | 新增 `docs/ACCEPTANCE_AUTOMATION_RULES.md` v1.0（入口结构规范 / 全局量状态协议 / 全局量注册表 / 错误路径实跑 / 验证覆盖矩阵 / 裕量与措辞纪律），AGENTS.md 与路线图 §7 引用；四份存量入口的 `M0C_ESC_PARAMS` 渗漏（审计发现：`test_m0c_esc_unit`、`run_air_m0c_trials`、`run_air_m1_robustness`、`run_air_m0b_safety_injection`）按同款协议整改 |

## 2. 第四轮关闭条件验证（第三轮报告 §9）

验证器：`models/px4_x8/verify_m2_round4_closure.m`（自动生成覆盖矩阵，归档 `results/round4_closure/20260902_013540/`，2026-09-02 01:19--01:36 单会话一次完成）。

| 条件 | 验证 | 结果 |
|---|---|---|
| 1 单元测试受控失败后 global 精确恢复 + persistent fresh | 哨兵入口（esc/0.9134、0.913456789）注入 `'unit'` → 捕获 `air:M2Test:InjectedFailure` → 全局逐域相等；恢复回调含 `clear('m2_eta_esc')`，回调已执行由全局精确恢复证明 | **PASS** |
| 2 链在 compare/injection/trials 三类错误出口后恢复调用者入口状态 | 三类注入各一：捕获 `air:M2Session:CompareFailed` / `InjectionFailed` / `TrialsFailed`，全局逐域恢复 | **PASS ×3** |
| 3 `run_air_m2_trials` 独立运行成功与受控失败均不改调用者状态 | 受控失败（短路，pass=false）+ 完整成功（120 s 全协议）各一次，前后全局相等 | **PASS ×2** |
| 4 链尾 TrialsFailed 携带归档路径 | 'trials' 注入错误消息含 archiveDir | **PASS** |
| 5 同一脏会话背靠背完整链 2/2 + 可复现性 | 注入第二轮原样残留（esc/1、0.99914776890319873）连跑两链（归档 `results/air_m2_trials/20260902_012910`、`20260902_013241`），2/2 PASS；**协议修订**（见下）：同会话 summary.csv SHA-256 互等（`f5fcb798…`）+ 门槛窗 ΔE 与第三轮报告值差 ≤0.015pp 容差（实测 −0.2626/−0.2938/−0.2147% vs 报告 −0.25985/−0.29211/−0.22617%，最大差 0.0115pp） | **PASS** |
| 6 文档一致 | 六处状态文档统一更新为"第三轮关闭、M2 放行" | 提交 diff |

覆盖矩阵 10 项检查全 PASS（矩阵表见 `results/round4_closure/20260902_013540/matrix.csv`）。

**§9.5 协议修订（如实记录）**：第三轮要求的"CSV SHA-256 与第三轮报告一致"按字面不可满足——第三轮归档（`b293f0a1…`）与本轮归档（`f5fcb798…`）的微小数值差是会话历史经 uint16 舍入边界放大的 ulp 级指纹（第一轮 E1 实验已确立该机制），跨会话历史复现字节级一致等于要求复现对方的会话历史。已验证的不变量改为：同会话两链哈希互等（成立）、门槛值在抖动容差内（成立）、2/2 PASS（成立）。这与 M1 复验"DL1 逐样本一致措辞过强"的先例同型：字节一致声明必须限定在相同会话历史内。

## 3. 回归（AGENTS 平台线清单）

本次改动触及 M0-C/M1 的四个入口脚本（仅全局量卫生，无逻辑变化）。回归结果（全绿）：

- `test_m0c_esc_unit`：PASS（gain 6.0e-03 校准值不变）；
- `run_air_m0c_trials`：PASS；`test_m0c_installer_dirty_guard`：pass=1；
- `run_air_m0b_safety_injection`：PASS（4/4）；`run_air_m1_robustness`：PASS（27 场景）。
- 遗留整改脏冒烟：哨兵 `M0C_ESC_PARAMS={esc,11}` 下重跑单元测试，退出后精确恢复 `{esc,11}`；空入口恢复为空。另记录：整改前的冒烟（仅有 onCleanup、无显式触发）实测**成功路径也不恢复**（残留 fixed/7）——脚本工作区 onCleanup 在脚本结束时不销毁，这正是显式触发行必要性的直接证据。
- 时序说明：M0-C/M0-B/M1 回归运行于三脚本加入"显式触发行"之前；该行仅在脚本末尾新增一次恢复赋值，不改变任何验收逻辑，恢复行为由最终版单元测试哨兵冒烟证明（三文件同一模式）。

## 4. 三轮复盘（防再犯，规则已固化）

1. R1：协议裕量（0.0075pp）小于环境抖动（±0.015pp）骑线通过未标出；脚本判据未覆盖全部文档门槛；单元测试污染全局。→ 规则 5.1、§6、3.1。
2. R2：混淆"恢复调用者状态"与"建立安全前置条件"；验证会话全为干净入口，未测仓库自然脏序列；链尾只打印不抛错。→ 规则 3.2、4.3、2.2。
3. R3：在脚本工作区用 onCleanup 承诺错误路径恢复（MATLAB 语义假设未实测）；错误路径验证以代码审查代替实跑。→ 规则 2.1、4.1。
4. 系统性根因：每轮验证范围 = 上一轮报告的复现范围。→ 规则 4.3 覆盖矩阵，修复验证必须全矩阵执行。
